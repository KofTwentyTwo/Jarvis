// Test fixture: Swift side deliberately declares BUS_PROTOCOL_VERSION = "2.0.0"
// while the TS side declares "1.9.0". The parity checker must refuse this pair.
public let BUS_PROTOCOL_VERSION: String = "2.0.0"
