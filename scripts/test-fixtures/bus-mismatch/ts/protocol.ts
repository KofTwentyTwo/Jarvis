// Test fixture: TS side deliberately declares BUS_PROTOCOL_VERSION = "1.9.0"
// while the Swift side declares "2.0.0". The parity checker must refuse this pair.
export const BUS_PROTOCOL_VERSION = "1.9.0";
