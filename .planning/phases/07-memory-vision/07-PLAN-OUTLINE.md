# Phase 07 — Plan Outline

Six plans across five waves. Plan 07-01 is already on disk (MEM-01 + MEM-02 schema/store skeleton); the remaining five plans cover the 11 pending REQ-IDs plus an integration/regression-corpus closer that wires AppDelegate and stands up the env-gated regression suite.

| Plan ID | Objective | Wave | Depends On | Requirements |
|---------|-----------|------|------------|--------------|
| 07-01 | Memory package skeleton + SQLite (WAL + FTS5 + sqlite-vec v0.1.10-alpha.3) + `MemoryConstants.embeddingDim = 768` single shared symbol + `facts` schema with `valid_from`/`valid_to`/`superseded_by`/`forgotten_at` columns + `vec_version()` smoke probe + `Replay.SQLiteConnection.withHandle` accessor. **(Already on disk — included for completeness; resume detector skips it.)** | 1 | [] | MEM-01, MEM-02 |
| 07-02 | Memory write path: `OllamaEmbeddingClient` against `/api/embed` for `nomic-embed-text` (768-dim, network-sandbox test asserts no non-localhost egress) + `MemoryExtractor` running mem0 ADD/UPDATE/NOOP via local `qwen2.5-coder:32b` Ollama with the system prompt template from RESEARCH §5 + `MemoryStore.applyOp` as the single emit site for `memory.mutated` ReplayEvent (supersede transaction per RESEARCH §6: insert new fact, set `valid_to` + `superseded_by` on prior, never DELETE) + `MemoryExtractionOrchestrator` as a separate `AgentOrchestrator` instance with `.memoryExtraction` `TurnSource`, bounded `AsyncChannel(capacity: 32)`, serial consumer Task that does NOT block `turnEnd`. Per D-01..D-04. | 2 | [01] | MEM-03, MEM-04, MEM-05, MEM-06 |
| 07-03 | Memory read path + MCP surfaces + DevOverlay: `MemoryStore.hybridSearch` implementing the FTS5 + vec0 RRF SQL from RESEARCH §8 (with `unicode61 remove_diacritics 2` tokenizer; `valid_to IS NULL OR valid_to > now` active-row filter; planner-tunable RRF k=60 constant) + `search_memory` (all-sessions per D-07), `search_conversation` (session-scoped TEXT-03 browse), and `forget_fact` (closes `valid_to` + sets `forgotten_at` per D-02; never deletes) MCP tools registered through `MCPRuntimeWiring.withMethodHandler` + `ReplayEvent.memoryRetrieval` case + DevOverlay `memory.used` row symmetric to `memory.mutated` (per D-05; both render as DevOverlay rows, no chat-panel chip per D-06) + chat-panel session-history browse plumbing for TEXT-03. Single emission site invariants enforced by grep gate. | 3 | [02] | MEM-07, MEM-08, TEXT-03 |
| 07-04 | Vision capture + presence (signal-only, VISION-03 enforced architecturally): new `packages/Vision` SPM target whose `Package.swift` does NOT depend on `JarvisTTS` or `JarvisOrchestrator` (CI check + grep gate enforces VISION-03 at compile time) + `CameraCapture` actor (AVCaptureSession-driven, single-frame-on-demand only, no continuous video buffer) + Camera TCC lifecycle via `AVCaptureDevice.authorizationStatus` with `degradationStream` graceful-denial pattern routed to `HUDBannerCoordinator` (mirrors AudioGraphOwner from Phase 6) + `PresenceMonitor` using `VNDetectFaceRectanglesRequest` with 2-second debounce on edge transitions and 5-minute "user gone" secondary threshold (per D-11) + `PresenceSignalBus` (consumed only by `ContextBuilder` for system-prompt enrichment per D-10 and by `HudStateCoordinator` for subtle ring indicator; no TTS/Orchestrator subscriber exists, asserted by grep gate) + `DisablePresence` UserDefaults-persisted menu-bar toggle cloning the `MuteWakeWord` shape from Phase 6 (per D-12; disabling presence does NOT disable frame-attach) + presence-on-by-default-after-first-TCC-grant behavior (D-09). | 2 | [01] | VISION-01, VISION-02, VISION-03 |
| 07-05 | Frame-attach + multimodal `LLMProvider` + T1/T2/T3 vision routing: extend `LLMProvider` protocol with `stream(messages:images:tools:toolChoice:)` signature (per D-18) + extend `TurnInput` to carry `[ImageBlock]` + add multimodal implementations to `OllamaProvider` (covers T1 `gemma4:31b` and reused for T2 against vllm-mlx port) and `AnthropicProvider` (T3 `claude-opus-4-7` `image_block` per Anthropic vision docs) + `VisionRouter` implementing the D-16 tier ladder with D-17 automatic T1→T2 escalation on low-confidence heuristic (planner-tuned substring set + length threshold) and explicit-only T1/T2→T3 escalation gated on user phrase ("send to Opus" / "use cloud") detected in `ContextBuilder` + `vllm-mlx` sidecar lifecycle managed via `ChildSpawnGate` (`FD_CLOEXEC` + minimal env, mirrors MCP helper spawn pattern; lazily spawned on first T2 need) with `@available(macOS 26)` runtime guard and feature-flag fallback to T3 if vllm-mlx unavailable + `FrameAttachController` driven by either phrase detection in `ContextBuilder` (regex per D-13, planner picks) or HUD camera-icon button (Bus message) + always-confirm thumbnail with `[Send] / [Cancel]` ~2-second affordance per D-14 + frame discard immediately after assistant turn completes per D-15 (replay records `{"type":"image","discarded":true}` placeholder, never raw bytes). | 4 | [04] | VISION-04 |
| 07-06 | Phase-7 integration closer (covers no new REQ-IDs; closes the wiring + verification gates): `AppDelegate.installMemory()` and `installVision()` entry points mirroring `installVoice()` + `MemoryExtractionOrchestrator` producer wired off `AgentOrchestrator.turnEnd` + `PresenceSignalBus` subscribers wired into `ContextBuilder` and `HudStateCoordinator` (verifying VISION-03 boundary still holds end-to-end) + `VisionRouter` wired into the live `Orchestrator.runTurn` path for `[ImageBlock]`-bearing turns + env-gated memory regression-test corpus per RESEARCH Open Q #3 (10-scenario suite under `JARVIS_REAL_MODELS=1`, asserts ADD/UPDATE/NOOP behavior, supersede invariants, FTS5+vec hybrid retrieval ranking, `forget_fact` closes valid_to without deleting; mirrors `OrpheusTTFATests` env-gated probe pattern from Phase 6) + post-merge `swift test` + `bash scripts/check-app-builds.sh` clean on develop + grep-gate sweep (`memory.mutated` single emission site; VISION-03 no-presence→TTS/Orchestrator path; `EMBEDDING_DIM` literal absent outside `MemoryConstants`). | 5 | [03, 05] | (integration only — no new REQ-IDs) |

## Coverage Notes

- **Pending REQ-ID coverage:** TEXT-03 → 07-03 ✓ | VISION-01..03 → 07-04 ✓ | VISION-04 → 07-05 ✓ | MEM-03..06 → 07-02 ✓ | MEM-07..08 → 07-03 ✓
- **Already on disk (07-01):** MEM-01, MEM-02
- **Locked decisions traced:** D-01..D-04 → 07-02; D-05..D-08 → 07-03; D-09..D-12 → 07-04; D-13..D-18 → 07-05
- **Wave DAG validity:** Wave 1 = [01]; Wave 2 = [02, 04] (parallel — disjoint scope: `packages/Memory` vs `packages/Vision`, no shared `files_modified`); Wave 3 = [03] (depends on 02); Wave 4 = [05] (depends on 04 for `packages/Vision` + on `LLMProvider` already in tree from P4); Wave 5 = [06] (depends on 03 + 05).
- **Parallelism:** 07-02 and 07-04 run in parallel (Wave 2). Both touch only their own package manifest plus already-existing P4/P5 surfaces in carefully disjoint ways: 07-02 touches `packages/Memory/*` and adds an `OllamaEmbeddingClient` to `packages/AgentCore/Sources/LLMProvider/` whose path does not overlap with 07-04's `packages/Vision/*` + `App/MenuBar/*` reach. Per-plan executor must restate exclusive `files_modified` lists.
- **Plan count:** 6 (matches budget cap from chunked-mode brief).

## OUTLINE COMPLETE

Plan count: 6
Pending REQ coverage: TEXT-03 ✓ | VISION-01..04 ✓ | MEM-03..08 ✓
