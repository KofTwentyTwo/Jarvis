---
context: phase
phase: 07-memory-vision
task: null
total_tasks: null
status: paused_mid_planning
workflow: gsd-plan-phase
workflow_step: "8 — gsd-planner spawn (interrupted by stream idle timeout)"
last_updated: 2026-04-29T13:24:08.501Z
last_commit_before_pause: 530130d
---

# Pause Snapshot — Phase 7 Planning (mid-planner)

> No blocking constraints discovered this session. Removing the BLOCKING CONSTRAINTS section.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Monolithic planner Task on a high-fidelity 6-plan phase | Single `gsd-planner` Task() with full CONTEXT (21 KB) + RESEARCH (18 KB) + PATTERNS (63 KB) + CLAUDE.md + ROADMAP + REQUIREMENTS pinned in the prompt timed out at ~19 min wall-clock / `tool_uses=45` after writing 1 of ~6 plans | advisory | Use `--chunked` for any phase whose suggested plan-count is ≥4 *or* whose total `<files_to_read>` payload exceeds ~80 KB. Outline Task is short (~2 min); per-plan Tasks are bounded (~3-5 min each) and committed individually. Plan 07-01's intact YAML frontmatter on disk acts as a resume marker — chunked mode skips it automatically. |

## Required Reading (in order)

1. `.planning/STATE.md` — current position, accumulated decisions, scaffold-time verifications, Phase 6 → Phase 8 deferred items.
2. `.planning/phases/07-memory-vision/07-CONTEXT.md` — D-01..D-18 locked decisions from `/gsd-discuss-phase 7`, "Claude's Discretion" enumerated. **Authoritative for Phase 7 scope.**
3. `.planning/phases/07-memory-vision/07-RESEARCH.md` — schema, SQL, mem0 prompt template, supersede transaction, background orchestrator pattern, hybrid search SQL with RRF, presence pipeline, Camera TCC lifecycle, package layout, pitfalls table. Note: no `## Validation Architecture` section — Dimension 8 must be satisfied by per-task commands.
4. `.planning/phases/07-memory-vision/07-PATTERNS.md` — 63 KB / 1,423 lines. 23 new + 7 modified surfaces mapped to existing analogs with absolute-path code excerpts. **Do NOT re-run pattern-mapper on resume.**
5. `.planning/phases/07-memory-vision/07-01-PLAN.md` — already-written first plan (43 KB, MEM-01 + MEM-02). Treated as completed by chunked-mode resume detection. Reference shape for the remaining 5 plans.
6. `.planning/HANDOFF.json` — structured machine-readable handoff for `/gsd-resume-work`.

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN: Re-spawning a non-chunked gsd-planner for the same phase** → it will time out again. Use `/gsd-plan-phase 7 --skip-ui --chunked`. Resume detection skips 07-01-PLAN.md.
- **ANTI-PATTERN: Re-running gsd-pattern-mapper on resume** → 07-PATTERNS.md is current and high-quality. Workflow §7.8 will detect it via the existing-PATTERNS check and skip.
- **ANTI-PATTERN: Re-asking the user about UI-SPEC or AI-SPEC** → those gates are already cleared this session (`--skip-ui` flag on resume; AI-SPEC convention follows Phases 4 + 6). The decision rationale is captured in HANDOFF.json `decisions[]`.

## Infrastructure State

- Branch: `develop` @ `530130d` (pushed; tree clean apart from listed uncommitted files).
- Subagent state: `agent_id: a904f050f84a65617` (planner) is dead — do NOT try to SendMessage to it. The pattern-mapper agent_id `a69de07181f83b104` is also unused on resume.
- No background processes. No external services depended on by the resume.

<current_state>
We are mid-`/gsd-plan-phase 7 --skip-ui` at workflow step 8 (planner spawn). Steps 1–7.8 completed cleanly:

- Init JSON loaded; phase validated; 07-CONTEXT.md (21 KB), 07-RESEARCH.md (290 lines), 07-DISCUSSION-LOG.md all present.
- VALIDATION.md gate: warn-and-continue (no `## Validation Architecture` in RESEARCH; matches Phase 6 precedent).
- UI-SPEC gate: cleared via user-provided `--skip-ui` (false-positive regex match on "ui" inside "requirements").
- AI-SPEC gate: skipped per Phase 4 + Phase 6 convention.
- Schema-push gate: no ORM file patterns matched (SQLite is direct via `sqlite3_load_extension`).
- gsd-pattern-mapper produced **07-PATTERNS.md** (63 KB / 1,423 lines / 23 new + 7 modified surfaces / 5 highest-risk scaffolds flagged). High quality.
- gsd-planner Task() spawned with full prompt covering 6-plan suggested decomposition. Wrote **07-01-PLAN.md** (43 KB, 4 tasks, full frontmatter with truths/artifacts/key_links/verification/success_criteria for MEM-01 + MEM-02) before stream idle timeout at ~19 min / tool_uses=45.

Plans 07-02 through 07-06 are NOT written. The remaining REQ-IDs are uncovered:

| Plan (suggested) | REQ-IDs uncovered | Wave (suggested) | depends_on |
|------------------|-------------------|------------------|------------|
| 07-02 Embedder + Extractor + Background orchestrator | MEM-03, MEM-04, MEM-05, MEM-06 | 2 | [01] |
| 07-03 Memory retrieval + MCP tools + DevOverlay surfaces | MEM-07, MEM-08, TEXT-03 | 3 | [02] |
| 07-04 Vision capture + Presence + SignalBus + menu-bar toggle | VISION-01, VISION-02, VISION-03 | 2 | [01] |
| 07-05 FrameAttach + multimodal LLMProvider + T1/T2/T3 routing | VISION-04 | 4 | [04] |
| 07-06 AppDelegate wiring + memory regression-test corpus | (integration; covers no new REQ-IDs) | 5 | [03, 05] |

13 phase REQ-IDs total: TEXT-03, VISION-01, VISION-02, VISION-03, VISION-04, MEM-01..MEM-08. Of these, MEM-01 and MEM-02 are covered by 07-01; the other 11 are pending plan coverage.
</current_state>

<completed_work>

This session (2026-04-28 → 2026-04-29):
- Workflow init + 5 pre-planner gates (Nyquist warn, UI skipped via flag, AI-SPEC skipped per convention, schema-push N/A, security gate non-blocking)
- gsd-pattern-mapper run to completion — `07-PATTERNS.md` written
- gsd-planner partial run — `07-01-PLAN.md` written, then stream idle timeout
- HANDOFF.json + .continue-here.md regenerated to reflect mid-planning pause
</completed_work>

<remaining_work>

- Resume planning via `--chunked` to write 07-02..07-06 (5 plans)
- Plan checker against the full plan set (max 3 revision iterations)
- Requirements coverage gate (13/13 REQ-IDs)
- Decision coverage gate (D-01..D-18 from CONTEXT.md)
- STATE.md / ROADMAP.md annotation + commit (`docs(07): create phase plan`)
- Post-planning gap analysis report (non-blocking)
</remaining_work>

<decisions_made>

- **Skip UI-SPEC** via `--skip-ui` — false-positive regex match on "ui" in "requirements"; Phase 7 UI surfaces are extensions to existing patterns + two small affordances under Claude's Discretion.
- **Skip AI-SPEC** — Phases 4 and 6 followed the same convention; D-01..D-18 fully specify the AI work.
- **Continue without VALIDATION.md** — workflow §5.5 warn-and-continue path; matches Phase 6 precedent. Plans must embed test commands directly.
- **Use `--chunked` on resume** — single-Task planner timed out at tool_uses=45. Chunked mode bounds each subagent and commits per-plan for crash resilience.
</decisions_made>

<blockers>

- **Stream idle timeout on monolithic gsd-planner Task()** — workaround: resume with `--chunked`. Not a permanent blocker; the chunked-mode skip-detection is the recovery path.
</blockers>

<context>
Mental model: Phase 7 splits cleanly along three independent feature seams — memory (MEM-01..08 + TEXT-03), vision presence (VISION-01..03), and vision frame-attach (VISION-04). Plan 07-01 nails the foundation: SPM package, schema, sqlite-vec extension load, single-symbol EMBEDDING_DIM. The remaining 5 plans should mirror its structure (must_haves with truths/artifacts/key_links + per-task read_first/action/acceptance_criteria + verification + success_criteria) and honor the highest-risk scaffolds 07-PATTERNS.md flagged:

1. MemoryStore extension load via direct C API (covered by 07-01 — done)
2. vllm-mlx sidecar lifecycle (lands in 07-05; mirror ChildSpawnGate spawn + Phase-6 OrpheusEngine warmup-probe pattern)
3. HUD camera-icon button + confirmation thumbnail (lands in 07-05; first image-rendering HUD modal — Claude's discretion design)
4. LLMProvider multimodal protocol extension (07-05; protocol-default forwarding for the 3 existing conformers)
5. VISION-03 compile-time boundary (07-04; Package.swift dependency exclusion + scripts/check-presence-vision-isolation.sh grep gate)

The phase has zero locked-in code yet — all clean greenfield except for surgical extensions to SQLiteConnection (Replay), AgentOrchestrator/TurnInput (AgentCore), AppDelegate.swift (App), HudStateCoordinator (App/HUD), ContextBuilder (AgentCore), and ReplayEvent (Replay). The 4 highest-risk scaffolds are all in 07-05 — that plan deserves the most planner attention.
</context>

<next_action>
Run: `/gsd-plan-phase 7 --skip-ui --chunked`

The chunked workflow path:
1. Spawns a short outline-mode planner (~2 min) producing `07-PLAN-OUTLINE.md` — a markdown table of remaining plans with id/objective/wave/depends_on/requirements columns.
2. Per-plan resume check: `07-01-PLAN.md` already on disk with intact YAML frontmatter → chunked mode SKIPS it. The outline should NOT include 07-01 (already done) and should explicitly list plans 07-02..07-06.
3. For each remaining plan, spawns a single-plan-mode planner Task (~3-5 min each), commits per-plan via `gsd-sdk query commit`.
4. After all plans land, treats it as `## PLANNING COMPLETE` and proceeds to step 9 (checker) → revision loop → coverage gates → commit.

If the outline mistakenly re-includes 07-01, manually edit `07-PLAN-OUTLINE.md` to remove that row before the per-plan Tasks fan out. Resume detection by file-existence-+-frontmatter is the safety net but the outline shape matters for downstream commits.
</next_action>
