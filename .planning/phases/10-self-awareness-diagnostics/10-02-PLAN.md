---
phase: 10-self-awareness-diagnostics
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - packages/AgentCore/Sources/AgentCore/ContextBuilder.swift
  - packages/AgentCore/Tests/AgentCoreTests/CacheHintsEligibilityTests.swift
  - App/AppDelegate.swift
  - .planning/phases/10-self-awareness-diagnostics/10-02-SUMMARY.md
autonomous: false
requirements: [SELF-05, SELF-06]
decisions_covered: [D-01, D-02, D-03, D-04, D-12, D-13, D-14, D-26, D-27, D-28, D-29, D-30, D-31]
must_haves:
  truths:
    - "Asking 'what mic are you using?' produces a get_active_audio_route tool call (visible in DevOverlay last-5 tool calls list once Plan 10-03 lands)"
    - "Asking 'are you a text-only assistant?' produces a denial response citing voice + vision capabilities"
    - "The hardcoded literal 'You are Jarvis, a personal macOS assistant.' no longer appears at App/AppDelegate.swift:1242 or anywhere in the App/ tree"
    - "ContextBuilder.selfAwarePreamble is a single locked Swift string constant (D-13) that does NOT enumerate the tool list as a Markdown block (D-14)"
    - "Adding the preamble does NOT push the system prompt over the Anthropic 4096-char cache-eligibility boundary unintentionally"
    - "All 18 boundary gates green at HEAD before plan SUMMARY.md commits (CLAUDE.md § Boundary gates)"
    - "bash scripts/check-app-builds.sh PASS at HEAD before plan SUMMARY.md commits"
    - "Live Verification section populated in SUMMARY.md per RESEARCH.md § Live-Launch Verification Format (D-26)"
    - "Test names follow Mock/Stub/Fake taxonomy from CLAUDE.md § Test naming conventions (D-31)"
    - "Plan structure honors D-01 (serial, no parallel-agent fan-out), D-02 (5 plans in foundation→UI order), D-03 (5 plans not 4 or 6), D-04 (every plan ends with Live Verification SUMMARY section)"
    - "Live-launch verification gate honors D-27 (tests-green is necessary but not sufficient), D-28 (tccutil reset protocol — N/A here, no hardware capture surface touched)"
    - "Boundary-gate discipline honors D-29 (18 existing boundary gates stay green; AppDelegate.swift change must not regress check-no-leftover-stubs.sh / check-install-order.sh), D-30 (no new boundary gate required — procedural enforcement)"
  artifacts:
    - path: "packages/AgentCore/Sources/AgentCore/ContextBuilder.swift"
      provides: "selfAwarePreamble static constant + systemPrompt(for:) composer (preamble FIRST, then presence, then memory)"
      contains: "selfAwarePreamble"
    - path: "App/AppDelegate.swift"
      provides: "AgentOrchestrator.init systemPrompt parameter wired to ContextBuilder.systemPrompt(for:) (or selfAwarePreamble fallback per D-12)"
      contains: "ContextBuilder"
    - path: "packages/AgentCore/Tests/AgentCoreTests/CacheHintsEligibilityTests.swift"
      provides: "preambleDoesNotEnableCacheUnintentionally test asserting cache-marker behavior with the preamble alone"
      contains: "preambleDoesNotEnableCacheUnintentionally"
  key_links:
    - from: "App/AppDelegate.swift line 1242"
      to: "ContextBuilder.selfAwarePreamble (or systemPrompt(for:))"
      via: "AgentOrchestrator(systemPrompt:) constructor argument"
      pattern: "ContextBuilder\\.(selfAwarePreamble|systemPrompt)"
    - from: "Live model"
      to: "get_active_audio_route tool (registered in Plan 10-01)"
      via: "Preamble nudge: 'always prefer calling those tools over giving generic instructions'"
      pattern: "get_active_audio_route"
---

<objective>
Replace the hardcoded `"You are Jarvis, a personal macOS assistant."` literal at `App/AppDelegate.swift:1242` with a self-aware preamble emitted by `ContextBuilder` that names Jarvis, lists capabilities (mic, camera, speakers, MCP tool surface), and instructs the model to PREFER the introspection tools (registered in Plan 10-01) over generic "open System Settings" answers.

Purpose: Closes the user-quoted failure modes "I'm Jarvis, your text-based assistant — I don't actually have a microphone" (2026-05-05) and "I don't have the ability to detect your hardware directly" (2026-05-05). Closes SELF-05/06 / AC-05/06.

Output:
- Static `selfAwarePreamble: String` constant on `ContextBuilder` (verbatim from RESEARCH §Example 5 / PATTERNS lines 192-203)
- Static `systemPrompt(for:)` composer that prepends the preamble to per-turn enrichments
- `AppDelegate.swift:1242` literal replaced with `ContextBuilder.systemPrompt(for: .empty)` (or `ContextBuilder.selfAwarePreamble` if the orchestrator init shape doesn't accept a per-turn context — D-12 fallback)
- Wave-0 test `CacheHintsEligibilityTests/preambleDoesNotEnableCacheUnintentionally` asserts the preamble alone does NOT push the system prompt across the 4096-char cache eligibility boundary
- Live verification: relaunch, ask "what mic are you using?" via voice or text, observe tool call (post-10-03 DevOverlay populated) + factual answer naming the device
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-self-awareness-diagnostics/10-SPEC.md
@.planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md
@.planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md
@.planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md
@.planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md
@CLAUDE.md
@.planning/phases/10-self-awareness-diagnostics/10-01-SUMMARY.md
@packages/AgentCore/Sources/AgentCore/ContextBuilder.swift
@packages/AgentCore/Sources/AgentCore/CacheHints.swift
@packages/AgentCore/Sources/AgentOrchestrator/AgentOrchestrator.swift
@packages/AgentCore/Tests/AgentCoreTests/CacheHintsEligibilityTests.swift

<interfaces>
ContextBuilder (packages/AgentCore/Sources/AgentCore/ContextBuilder.swift):
- existing struct — DO NOT REFACTOR
- Plan 10-02 ADDS via extension: `selfAwarePreamble: String` constant + `systemPrompt(for: TurnContext) -> String`

CacheHints (packages/AgentCore/Sources/AgentCore/CacheHints.swift:32-56):
- 4096-char threshold for `eligibleForSystemPrompt` cache-marker emission
- Plan 10-02 keeps preamble alone < 2 KB so combined with rare presence/memory enrichments it stays under 4 KB

AgentOrchestrator init (packages/AgentCore/Sources/AgentOrchestrator/AgentOrchestrator.swift:38, 98-108, 254):
- Currently takes `systemPrompt: String` directly (per RESEARCH line 675)
- Plumbing per-turn TurnContext through orchestrator init is out of scope (D-12 fallback)

Current literal site (App/AppDelegate.swift:1242):
```
systemPrompt: "You are Jarvis, a personal macOS assistant.",
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Extend CacheHintsEligibilityTests with preambleDoesNotEnableCacheUnintentionally (RED)</name>
  <read_first>
    - packages/AgentCore/Tests/AgentCoreTests/CacheHintsEligibilityTests.swift (existing — extend, do not rewrite)
    - packages/AgentCore/Sources/AgentCore/CacheHints.swift (lines 32-56 — 4096-char threshold logic)
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (Pitfall #4 + Assumption A4 — cache-eligibility constraint)
    - .planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md (lines 213-214 — Cache-eligibility constraint)
    - .planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md (Per-Task Verification Map — SELF-06 / T-10-CACHE-01 row)
  </read_first>
  <behavior>
    - Test asserts: ContextBuilder.selfAwarePreamble length stays well below the 4096-char cache boundary (under 2048 chars)
    - Test asserts: when the system prompt = preamble alone, CacheHints.eligibleForSystemPrompt(...) returns the same value (nil OR cache-marker, whichever the existing impl returns for the OLD literal "You are Jarvis, a personal macOS assistant."). The preamble does not FLIP cache-marker emission compared to the previous tiny literal.
    - Test asserts: ContextBuilder.systemPrompt(for: TurnContext.empty) returns a string starting with the selfAwarePreamble (preamble FIRST, per D-12).
    - Test name preambleDoesNotEnableCacheUnintentionally per VALIDATION.md row.
    - Test doubles named per D-31 (Stub*, Mock*, or Fake* — for this test, no doubles needed; pure constant comparison).
  </behavior>
  <action>
    Edit packages/AgentCore/Tests/AgentCoreTests/CacheHintsEligibilityTests.swift. Add a single @Test (Swift Testing) named preambleDoesNotEnableCacheUnintentionally:

    Test body asserts (verbatim):
    1. ContextBuilder.selfAwarePreamble.count is less than 2048 (well below 4096-char cache boundary; Pitfall #4 / A4)
    2. ContextBuilder.selfAwarePreamble starts with the prefix "You are Jarvis"
    3. ContextBuilder.selfAwarePreamble does not contain a markdown code fence triple-backtick (D-14 — no tool catalog enumeration as block)
    4. CacheHints.eligibleForSystemPrompt(priorLiteral) equals CacheHints.eligibleForSystemPrompt(preamble), where priorLiteral is "You are Jarvis, a personal macOS assistant." — i.e., the preamble does not unintentionally flip cache-eligibility from the prior literal

    If CacheHints.eligibleForSystemPrompt has a different signature than (String) -> Something?, adapt the call site to match the actual API found in packages/AgentCore/Sources/AgentCore/CacheHints.swift. The shape of the assertion stays the same: BEFORE-LITERAL eligibility equals AFTER-PREAMBLE eligibility.

    Run `swift test --package-path packages/AgentCore --filter CacheHintsEligibilityTests/preambleDoesNotEnableCacheUnintentionally`. The test MUST fail to compile (RED) because ContextBuilder.selfAwarePreamble does not exist yet.
  </action>
  <verify>
    <automated>cd /Users/james.maes/Git.Local/Kof22/Jarvis &amp;&amp; swift test --package-path packages/AgentCore --filter CacheHintsEligibilityTests/preambleDoesNotEnableCacheUnintentionally 2>&amp;1 | grep -E "(error:|cannot find)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "preambleDoesNotEnableCacheUnintentionally" packages/AgentCore/Tests/AgentCoreTests/CacheHintsEligibilityTests.swift` returns at least 1
    - `grep -c "ContextBuilder.selfAwarePreamble" packages/AgentCore/Tests/AgentCoreTests/CacheHintsEligibilityTests.swift` returns at least 1
    - `swift test --package-path packages/AgentCore --filter CacheHintsEligibilityTests/preambleDoesNotEnableCacheUnintentionally` produces a compile error mentioning `selfAwarePreamble`
  </acceptance_criteria>
  <done>One new @Test case added, failing to compile (RED). Test name matches VALIDATION.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ContextBuilder.selfAwarePreamble + systemPrompt(for:) and rewire AppDelegate (GREEN)</name>
  <read_first>
    - packages/AgentCore/Sources/AgentCore/ContextBuilder.swift (current body — extend via extension; do not refactor existing API)
    - packages/AgentCore/Sources/AgentOrchestrator/AgentOrchestrator.swift (lines 38, 98-108, 254 — verify init shape; if it takes systemPrompt: String directly, the per-turn TurnContext composition is out of scope per D-12 fallback)
    - App/AppDelegate.swift (line 1242 ± 20 — current literal site; read surrounding lines to confirm the call shape)
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 640-672 — Example 5 verbatim skeleton)
    - .planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md (lines 188-238 — Plan 10-02 subsections)
    - .planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md (D-12, D-13, D-14 — preamble locks)
  </read_first>
  <action>
    1. Extend ContextBuilder.swift via a public extension. Add (verbatim from RESEARCH Example 5 lines 640-672):

       - public static let selfAwarePreamble: String — multi-line Swift string literal beginning "You are Jarvis, an always-on macOS assistant running as a Swift app on the user's Mac." and including: voice input via mic, voice output via speakers, camera available, the inline list of tool names "(list_audio_devices, get_active_audio_route, get_self_state, list_camera_devices, get_time, get_clipboard, run_applescript)", the "always prefer" instruction, and the closing "You are not a text-only assistant." sentence. Use Swift's """ triple-quote multi-line literal with backslash-line-continuation. Total length under 2048 chars.

       - public static func systemPrompt(for turn: TurnContext) -> String composing in this exact order: selfAwarePreamble, then "\n\n" + presenceText if non-nil, then "\n\n" + memoryHydration if non-nil. Two distinct concatenation steps; do not collapse them (D-12).

       If TurnContext already exists with different field names (presenceText / memoryHydration), adapt the access expressions but keep the order: preamble → presence → memory. If TurnContext does not exist or doesn't carry those fields, define a minimal `public struct TurnContext: Sendable { public let presenceText: String?; public let memoryHydration: String?; public init(presenceText: String? = nil, memoryHydration: String? = nil); public static let empty = TurnContext() }` so the composer compiles. Do NOT plumb new dependencies into AgentOrchestrator (out of scope per D-12 fallback).

    2. Replace App/AppDelegate.swift:1242. Read the surrounding 30 lines first to confirm the AgentOrchestrator init call shape, then replace the literal with one of (in order of preference):

       Preferred (TurnContext available with .empty):
       `systemPrompt: ContextBuilder.systemPrompt(for: .empty),`

       Fallback (per D-12 — orchestrator takes systemPrompt at init only, no per-turn re-injection wired in this phase):
       `systemPrompt: ContextBuilder.selfAwarePreamble,`

       Pick the simplest variant that compiles. Behavior is identical at orchestrator-init: the preamble flows in. Per-turn presence enrichment was already separate (PresenceStateSnapshot.shared lives in the orchestrator init args; not collapsed into systemPrompt here).

    3. Verify the prior literal is GONE: `grep -rn "You are Jarvis, a personal macOS assistant" App/ packages/` MUST return zero matches.

    4. Build the App + run the AgentCore test suite. The Wave-0 test from Task 1 MUST flip RED → GREEN.

    5. Run all 18 boundary gates. None should regress (this change is purely additive on the AgentCore side and a single-line replacement on the App side).
  </action>
  <verify>
    <automated>cd /Users/james.maes/Git.Local/Kof22/Jarvis &amp;&amp; swift test --package-path packages/AgentCore --filter CacheHintsEligibilityTests &amp;&amp; bash scripts/check-app-builds.sh &amp;&amp; bash scripts/check-no-leftover-stubs.sh &amp;&amp; bash scripts/check-install-order.sh &amp;&amp; bash scripts/check-applescript-confirmation.sh &amp;&amp; bash scripts/check-no-evaluate-javascript.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "selfAwarePreamble" packages/AgentCore/Sources/AgentCore/ContextBuilder.swift` returns at least 1
    - `grep -c "static func systemPrompt(for" packages/AgentCore/Sources/AgentCore/ContextBuilder.swift` returns at least 1
    - `grep -c "You are Jarvis, an always-on macOS assistant" packages/AgentCore/Sources/AgentCore/ContextBuilder.swift` returns 1
    - `grep -c "list_audio_devices" packages/AgentCore/Sources/AgentCore/ContextBuilder.swift` returns 1 (preamble names the tool inline per D-14)
    - `grep -rn "You are Jarvis, a personal macOS assistant" App/ packages/` returns no matches
    - `grep -c "ContextBuilder" App/AppDelegate.swift` returns at least 1
    - `swift test --package-path packages/AgentCore --filter CacheHintsEligibilityTests` exits 0 (Wave-0 test now GREEN)
    - `bash scripts/check-app-builds.sh` exits 0
    - All 18 boundary gates exit 0
  </acceptance_criteria>
  <done>Preamble composed; AppDelegate uses ContextBuilder. Cache-eligibility test GREEN. All boundary gates green. Prior literal eliminated from the codebase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification — relaunch + ask introspection questions + observe correct answers</name>
  <read_first>
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 968-998 — Live-Launch Verification Format D-26 template; copy verbatim into 10-02-SUMMARY.md)
    - .planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md (Manual-Only Verifications table — "What mic are you using?" routes to get_active_audio_route; "Are you a text-only assistant?" produces denial)
    - .planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md (D-26, D-27, D-28 — tests-green not sufficient; tccutil reset N/A for this plan because no hardware capture surface)
  </read_first>
  <what-built>
    - ContextBuilder.selfAwarePreamble + systemPrompt(for:) composer
    - AppDelegate orchestrator-init systemPrompt now sourced from ContextBuilder (replacing the "You are Jarvis, a personal macOS assistant." literal)
    - CacheHintsEligibilityTests/preambleDoesNotEnableCacheUnintentionally GREEN
    - All 18 boundary gates green
  </what-built>
  <how-to-verify>
    Per D-26 template — copy the section verbatim into .planning/phases/10-self-awareness-diagnostics/10-02-SUMMARY.md.

    Plan 10-02 does NOT touch hardware capture surfaces directly (it's a string-constant change + a single-line orchestrator init). Per D-28 guidance, plans 10-01 and 10-02 do not strictly require tccutil reset. In the SUMMARY's Reset block include: "Reset: N/A — Plan 10-02 modifies system prompt composition only; no hardware capture surface touched."

    Pre-checks:
    1. `bash scripts/check-app-builds.sh` PASS
    2. All 18 boundary gates green
    3. `swift test --package-path packages/AgentCore` PASS

    Relaunch:
    - `bash scripts/check-app-builds.sh && open build/Build/Products/Debug/Jarvis.app`
    - Capture build artifact path + commit short-sha at `git rev-parse --short HEAD`

    Live model behavior tests (run BOTH; both are required for AC-05/AC-06):

    Test 1 (AC-05): Send a turn — voice OR text — asking exactly: "What microphone are you using?"
    - EXPECTED behavior A: model invokes `get_active_audio_route` (visible in DevOverlay last-5 tool calls AFTER Plan 10-03 lands; if 10-03 is not yet shipped, observe via the agent log channel `subsystem == "com.koftwentytwo.jarvis" category == "agent"` for `tool_use` events naming the tool)
    - EXPECTED behavior B: response cites the actual device name returned by the tool (e.g., "MacBook Pro Microphone")
    - NOT acceptable: response saying "open System Settings" or "I don't have access to your hardware" or "I'm a text-based assistant"

    Test 2 (AC-06): Send a turn asking exactly: "Are you a text-only assistant?"
    - EXPECTED: response cites voice + vision capabilities and explicitly denies being text-only (the preamble's last sentence "You are not a text-only assistant." should anchor this)

    Capture both turns' transcripts (request + response) verbatim into the SUMMARY.

    Note on AC-05 cross-plan dependency: Plan 10-03 (DevOverlay subscriber fix) is the natural visualizer for "did the tool actually fire?". If 10-02 ships before 10-03, fall back to log inspection: `log stream --predicate 'subsystem == "com.koftwentytwo.jarvis"' --style compact | grep -E "(tool_use|tool_result|get_active_audio_route)"` during the live turn. Document which path was used in the SUMMARY.

    Write the populated `## Live Verification (D-26 / SPEC AC-13)` section into 10-02-SUMMARY.md. The block MUST contain:
    - Pre-checks block (3 items checked)
    - Reset block (N/A note)
    - Relaunch block with build artifact + commit short-sha
    - Observed block with the actual question asked + actual response received + tool-call evidence (DevOverlay screenshot or log line)
    - Acceptance Match block: `AC-05: PASS — <evidence>`, `AC-06: PASS — <evidence>`
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/10-self-awareness-diagnostics/10-02-SUMMARY.md` contains the heading `## Live Verification (D-26 / SPEC AC-13)`
    - SUMMARY contains both `AC-05: PASS` AND `AC-06: PASS`
    - SUMMARY contains the verbatim user question "What microphone are you using?" AND the verbatim response received
    - SUMMARY contains the verbatim user question "Are you a text-only assistant?" AND the verbatim response received
    - SUMMARY contains either a DevOverlay last-5-tool-calls reference (post-10-03) or a log-stream excerpt mentioning `get_active_audio_route`
    - All 18 boundary gates exit 0 at the commit-of-record (re-run after writing SUMMARY)
  </acceptance_criteria>
  <resume-signal>Type "approved" once both AC-05 and AC-06 PASS in the Live Verification section. If the model still says "I'm a text-only assistant" or directs the user to System Settings, describe the exact response so the executor can iterate on the preamble wording (within D-13's locked constraints).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ContextBuilder → AgentOrchestrator | Preamble (Swift-side trusted) flows into the model's `system` prompt array. No untrusted input crosses here. |
| AgentOrchestrator → Anthropic API | System prompt is sent over TLS to api.anthropic.com. Existing AGENT-02 cache_control + extended-cache-ttl-2025-04-11 header invariants apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-CACHE-01 | Denial of Service / Cost Abuse | Preamble pushes system prompt over Anthropic 4096-char cache-eligibility boundary | mitigate | Preamble alone is < 2 KB (verified by `preambleDoesNotEnableCacheUnintentionally` test asserting `selfAwarePreamble.count < 2048`). Combined with rare per-turn presence/memory enrichments, total stays under 4 KB. The test asserts cache-eligibility verdict for the preamble alone matches the verdict for the prior tiny literal — same cache-marker behavior, no surprise cache-creation cost. Per RESEARCH Pitfall #4 + Assumption A4. |
| T-10-PROMPT-01 | Tampering | Preamble content drift via accidental edit | mitigate | Preamble is a single locked string constant in code (D-13). No JSON file, no localization, no runtime mutation. Test `preambleDoesNotEnableCacheUnintentionally` enforces hasPrefix("You are Jarvis") and absence of triple-backtick (D-14 — no Markdown tool catalog block). |
| T-10-PROMPT-02 | Information Disclosure | Preamble leaking sensitive runtime detail | accept | Preamble names tool surface (`list_audio_devices`, `get_active_audio_route`, etc.) which are already exposed via the Anthropic tools array per turn. No keys, no PII, no transcript content. The model already has access to this list. Risk accepted. |
</threat_model>

<verification>
- `swift test --package-path packages/AgentCore --filter CacheHintsEligibilityTests/preambleDoesNotEnableCacheUnintentionally` PASS (Wave-0 test GREEN)
- `bash scripts/check-app-builds.sh` PASS
- All 18 boundary gates exit 0
- `grep -rn "You are Jarvis, a personal macOS assistant" App/ packages/` returns no matches (prior literal eliminated)
- `.planning/phases/10-self-awareness-diagnostics/10-02-SUMMARY.md` contains a populated `## Live Verification` section with PASS evidence for AC-05 + AC-06
</verification>

<success_criteria>
- ContextBuilder owns the self-aware preamble as a single locked string constant; systemPrompt(for:) composes preamble FIRST, then per-turn enrichments
- AppDelegate.swift orchestrator-init no longer carries the literal "You are Jarvis, a personal macOS assistant."
- Live model answers "what mic are you using?" with a tool call to get_active_audio_route + a factual device-named response
- Live model denies being a text-only assistant when asked, citing voice + vision
- All 18 boundary gates green; cache-eligibility verdict for the preamble alone matches the prior literal's verdict (no unintended cache-marker flip)
</success_criteria>

<output>
After completion, create `.planning/phases/10-self-awareness-diagnostics/10-02-SUMMARY.md` with:
- Standard summary template (objective, work delivered, files touched, tests added/changed, gates run)
- A `## Live Verification (D-26 / SPEC AC-13)` section per the canonical template at `10-RESEARCH.md` lines 968-998 with AC-05 + AC-06 marked PASS and verbatim question/response evidence inline
</output>
