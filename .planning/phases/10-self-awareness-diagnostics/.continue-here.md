---
context: phase
phase: 10-self-awareness-diagnostics
task: 0
total_tasks: 0
status: paused
last_updated: 2026-05-06T18:34:51.749Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure during 2026-05-04..06.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Tests-green ≠ production-works** — Today's session repeatedly produced agents shipping passing unit/integration tests for code paths that were inert at runtime (mic delivered silence, TTS was mute, DevOverlay subscriber received nothing, Voice Log empty, model claimed it had no audio hardware). Structural mitigation: SPEC AC-13 makes a documented live-launch + observed-log-capture step a hard gate at the end of every plan in Phase 10.
- [ ] CONSTRAINT: **Parallel agents on overlapping files produce partial work** — Today's 4-agent parallel dispatch (camera, DevOverlay, Voice Log, wake-word) produced 1 clean commit and 3 incomplete piles of uncommitted edits + new files that compiled but didn't function. Structural mitigation: Phase 10 plans run serially, single-threaded, with explicit live verification at each plan boundary. Stashed partial work is at `stash@{0}`; review for salvage before discarding.
- [ ] CONSTRAINT: **TCC permissions require explicit `requestAccess` calls** — Info.plist usage strings + AVAudioEngine.start() are not enough on macOS. Without `AVCaptureDevice.requestAccess(for: .audio | .video)`, the OS does not show the prompt and the app silently receives empty input. Structural mitigation: `installVoice` and `installVision` now make explicit requestAccess calls (b92d062). Future hardware integrations must do the same at first-use.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Tests-green ≠ production-works | Audit + parallel agents both shipped passing tests for inert production paths | blocking | SPEC AC-13: every plan ends with documented live-launch + log capture |
| Parallel agents on shared files | 3 of 4 simultaneous agents produced partial work that compiled but didn't function | blocking | Phase 10 plans run serially; one agent at a time; live verify at each boundary |
| Silent TCC fail | Info.plist alone doesn't trigger the OS permission dialog on macOS | advisory | Pattern in b92d062: explicit AVCaptureDevice.requestAccess at first-use boundary |
| Beta header hypothesis-without-evidence | Speculative gating of `anthropic-beta` header was wrong; real cause was missing `stream: true` in body | advisory | TDD discipline: write a test that captures the broken request shape, then fix from there |

<current_state>
Phase 10 SPEC.md committed at `c8e37ab`. ROADMAP.md updated with Phase 10 entry. develop pushed to origin, working tree clean. Three runtime bugs from today's live-launch session fixed tactically and pushed: d8983af (Anthropic stream:true), cb7ee6f (mic-regrant phantom rebuild), b92d062 (explicit TCC requests + honest wizard copy).

Voice + camera permission requests now exist but the actual hardware flow has not been live-verified end-to-end (user did not speak during my 20s log-stream window). Phase 10 SPEC explicitly addresses the structural gaps that made this debugging session necessary.
</current_state>

<completed_work>

Completed Tasks (today's session):
- Tactical fix: streamTruncatedFinal — added `"stream": true` to Anthropic request body (commit `d8983af`)
- Tactical fix: wake-word stream killed within 2s by phantom mic-regrant rebuild — primed lastMicStatus from current AVCaptureDevice auth (commit `cb7ee6f`)
- Tactical fix: TCC permissions never requested — added explicit AVCaptureDevice.requestAccess for mic + camera in installVoice/installVision; updated wizard copy from "(later)" to "(auto-prompts shortly)" (commit `b92d062`)
- Phase 10 SPEC.md written and committed (commit `c8e37ab`): 10 requirements, 14 acceptance criteria, ambiguity 0.18 (gate passed)
- ROADMAP.md updated with Phase 10 entry
</completed_work>

<remaining_work>

Phase 10 lifecycle:
- /gsd-discuss-phase 10 — implementation decisions (HOW). Recommended: --auto since SPEC.md is comprehensive
- /gsd-plan-phase 10 — produce RESEARCH.md + 4-6 plans
- /gsd-execute-phase 10 — wave-based execution; serial agents only (per constraint above); live-launch verification at every plan boundary
- /gsd-verify-phase 10 — goal-backward verification against the 14 acceptance criteria
</remaining_work>

<decisions_made>

- Phase 10 named "Self-Awareness & Live Diagnostics" with two-track scope: 6 self-knowledge MCP tools (SELF-01..06) + 4 live diagnostic UI surfaces (DIAG-01..04)
- Voice Log surfaced as AppKit window (not webview) — avoids widening WKContentWorld discipline; mirrors DevOverlay pattern
- Self-knowledge tools surfaced via MCP (not Bus) — tool calls are how the model already reaches into Jarvis; Bus changes would require schema widening (out of scope)
- Camera button click → frame attach: kept as a verification-only step (b92d062 already addressed the upstream TCC cause); not a Phase 10 requirement
- Live-launch verification baked into SPEC AC-13 as a hard gate per plan — direct response to today's tests-green-but-broken pattern
- Out of scope explicitly: tier-2 Orpheus TTS, vec0.dylib bundling, Ollama model pulls, AppDelegate.swift split refactor, Bus schema changes
</decisions_made>

<blockers>
- Wake-word fire still unverified post-b92d062 — user didn't speak during my 20s log-stream window. Plan 1 must include a real "Hey Jarvis" test in its verification step.
- TTS audible playback unverified — Jarvis voice output is mute even though tier-1 AVSpeechSynthesizer is structurally wired. DIAG-04 addresses this directly.
- Stashed partial work at stash@{0} from failed parallel dispatch (`partial-work-from-failed-parallel-dispatch-2026-05-06`). Files: App/AppDelegate.swift edits, App/MenuBar/MenuBarContextMenu.swift edits, project.yml/pbxproj edits, App/Voice/VoiceLogAdapters.swift, packages/VoiceLog/, partial test files. Likely salvageable scaffolding for DIAG-01 (Voice Log) and DIAG-02 (DevOverlay); review when planning those plans.
</blockers>

## Required Reading (in order)

1. `.planning/phases/10-self-awareness-diagnostics/10-SPEC.md` — locked requirements + boundaries + acceptance criteria
2. `docs/SESSION-STATE.md` — running ledger of today's commits and observed runtime state
3. `ARCHITECTURE.md` — voice + vision + MCP subsystem deep-dives
4. `.planning/audit-2026-05-04/SYNTHESIS.md` — six-auditor view of why "structurally sound" wasn't enough
5. `git log --oneline d8983af cb7ee6f b92d062 c8e37ab` — read each commit body; they document the live-launch debugging story

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: dispatching multiple parallel agents that each touch AppDelegate or other shared files → STRUCTURAL: serial agents only in Phase 10; one plan at a time
- ANTI-PATTERN: declaring a fix "complete" because tests pass without relaunching the app → STRUCTURAL: SPEC AC-13 hard-gates every plan on documented relaunch + observed-log capture
- ANTI-PATTERN: hypothesizing root cause based on log message wording rather than capturing the actual broken artifact → STRUCTURAL: TDD discipline — capture the broken request/state in a test that fails, then fix from there
- ANTI-PATTERN: assuming Info.plist usage strings trigger TCC prompts on macOS → STRUCTURAL: explicit AVCaptureDevice.requestAccess(for:) calls at first-use boundary

## Infrastructure State

- Jarvis app: PID 16819 (or fresh — likely killed/restarted by user testing)
- develop branch: `c8e37ab`, pushed to origin, working tree clean
- TCC state: mic + camera reset earlier; user click-Allowed mic during today's run; camera prompt fires at first vision use
- Boundary gates: 18 enforced, all green at HEAD
- Build cache: `build/Build/Products/Debug/Jarvis.app` is fresh from b92d062

## Pre-Execution Critique Required

N/A — no design artifact between phases yet. Phase 10 design (SPEC.md) was written today; next step is /gsd-discuss-phase 10 to lock implementation decisions, then /gsd-plan-phase 10 (which itself writes RESEARCH.md and plans).

<context>
The mental shift from this session: GSD discipline is not bureaucracy. The structured workflow (SPEC → DISCUSS → RESEARCH → PLAN → EXECUTE → VERIFY) exists precisely because the alternative — autonomous parallel agents producing apparently-complete work — produced today's chaos: a wake-word DAG that killed itself within 2 seconds of every launch, an Anthropic provider that returned 200+0bytes, a wizard that promised mic permission "later" but never delivered, a Voice Log window always empty, a DevOverlay always zero, and a model that thought it was a text-only assistant.

Phase 10's SPEC explicitly addresses the structural cause: every plan ends with live-launch verification, not just unit tests. Serial execution, not parallel. One agent at a time. Document the relaunch evidence in each SUMMARY.md.

The user's framing is correct: Jarvis must know 100% about itself and the host it runs on. That's not a UX nicety — it's the load-bearing trust signal that the agent IS what it claims to be. Self-knowledge tools + a self-aware system prompt make wrong answers ("I don't have a microphone") impossible by construction.
</context>

<next_action>
Start with: `/gsd-discuss-phase 10 --auto` to lock implementation decisions in a single pass (SPEC.md is comprehensive enough to drive sensible defaults), then `/gsd-plan-phase 10` to produce RESEARCH.md + 4-6 plans. Each plan must end with documented live-launch verification per SPEC AC-13. Run plans serially via /gsd-execute-phase, never in parallel.
</next_action>
