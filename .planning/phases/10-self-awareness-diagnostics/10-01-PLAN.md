---
phase: 10-self-awareness-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/MCP/Sources/MCP/InProcess/ListAudioDevicesTool.swift
  - packages/MCP/Sources/MCP/InProcess/GetActiveAudioRouteTool.swift
  - packages/MCP/Sources/MCP/InProcess/GetSelfStateTool.swift
  - packages/MCP/Sources/MCP/InProcess/ListCameraDevicesTool.swift
  - packages/MCP/Tests/MCPTests/ListAudioDevicesToolTests.swift
  - packages/MCP/Tests/MCPTests/GetActiveAudioRouteToolTests.swift
  - packages/MCP/Tests/MCPTests/GetSelfStateToolTests.swift
  - packages/MCP/Tests/MCPTests/ListCameraDevicesToolTests.swift
  - packages/MCP/Tests/MCPTests/MCPRuntimeWiringTests.swift
  - packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift
  - App/MCP/InProcessSelfStateAdapters.swift
  - App/MCP/MCPRuntimeWiring.swift
  - App/AppDelegate.swift
  - .planning/phases/10-self-awareness-diagnostics/10-01-SUMMARY.md
autonomous: true
requirements: [SELF-01, SELF-02, SELF-03, SELF-04]
decisions_covered: [D-01, D-02, D-03, D-04, D-07, D-08, D-09, D-10, D-11, D-26, D-27, D-28, D-29, D-30, D-31]
must_haves:
  truths:
    - "Calling list_audio_devices via the MCP runtime returns ≥1 input + ≥1 output entry whose UIDs match `system_profiler SPAudioDataType`"
    - "Calling get_active_audio_route while the voice loop is active returns a non-nil route whose sampleRate/channels match the AudioGraph 'probed format' log line"
    - "Calling get_self_state returns app_version, pid, uptime_seconds, llm_provider, llm_model, voice_loop_state with all fields populated"
    - "Calling list_camera_devices returns the built-in FaceTime camera entry (uniqueID matches `system_profiler SPCameraDataType`)"
    - "All four tools register with requiresConfirmation: false in MCPRuntimeWiring (per D-10)"
    - "All 18 boundary gates green at HEAD before plan SUMMARY.md commits (CLAUDE.md § Boundary gates)"
    - "bash scripts/check-app-builds.sh PASS at HEAD before plan SUMMARY.md commits"
    - "Live Verification section populated in SUMMARY.md per RESEARCH.md § Live-Launch Verification Format (D-26)"
    - "Test names follow Mock/Stub/Fake taxonomy from CLAUDE.md § Test naming conventions (D-31)"
    - "Plan structure honors D-01 (serial, no parallel-agent fan-out), D-02 (5 plans in foundation→UI order), D-03 (5 plans not 4 or 6), D-04 (every plan ends with Live Verification SUMMARY section)"
    - "Self-knowledge tool implementation honors D-07 (InProcessMemoryAdapters.swift Track D-2 pattern), D-08 (CoreAudio synchronous), D-09 (no caching of device lists — fresh syscall every call), D-11 (get_self_state is point-in-time snapshot of Bundle/ProcessInfo/LLMProvider/PerTurnSnapshot/VoiceController.state)"
    - "Live-launch verification gate honors D-27 (tests-green is necessary but not sufficient — verify-phase rejects plans with missing Live Verification section), D-28 (tccutil reset protocol when touching mic/camera/audio — N/A here, read-only metadata only)"
    - "Boundary-gate discipline honors D-29 (18 existing boundary gates stay green at every plan boundary; new MCP tool registration must not regress check-applescript-confirmation.sh), D-30 (no new boundary gate required — procedural enforcement via SUMMARY format)"
  artifacts:
    - path: "packages/MCP/Sources/MCP/InProcess/ListAudioDevicesTool.swift"
      provides: "list_audio_devices InProcessTool with requiresConfirmation=false"
      contains: "public let name = \"list_audio_devices\""
    - path: "packages/MCP/Sources/MCP/InProcess/GetActiveAudioRouteTool.swift"
      provides: "get_active_audio_route InProcessTool"
      contains: "public let name = \"get_active_audio_route\""
    - path: "packages/MCP/Sources/MCP/InProcess/GetSelfStateTool.swift"
      provides: "get_self_state InProcessTool"
      contains: "public let name = \"get_self_state\""
    - path: "packages/MCP/Sources/MCP/InProcess/ListCameraDevicesTool.swift"
      provides: "list_camera_devices InProcessTool"
      contains: "public let name = \"list_camera_devices\""
    - path: "App/MCP/InProcessSelfStateAdapters.swift"
      provides: "CoreAudioDeviceListAdapter, AudioGraphRouteAdapter, SelfStateAdapter, AVCaptureDeviceListAdapter — App→Module bridge per Track D-2 pattern"
      min_lines: 100
    - path: "packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift"
      provides: "Additive activeRouteSnapshot() actor accessor returning (inputUID, inputName, outputUID, outputName, sampleRate, channels)"
      contains: "activeRouteSnapshot"
    - path: "App/MCP/MCPRuntimeWiring.swift"
      provides: "Registration of four self-knowledge tools via InProcessToolRegistry (or equivalent install path that owns AudioGraphOwner reference)"
      contains: "list_audio_devices"
  key_links:
    - from: "App/MCP/MCPRuntimeWiring.swift"
      to: "InProcessToolRegistry"
      via: "await registry.register(...) for each of four tools"
      pattern: "list_audio_devices|get_active_audio_route|get_self_state|list_camera_devices"
    - from: "App/MCP/InProcessSelfStateAdapters.swift"
      to: "AudioGraphOwner"
      via: "AudioGraphRouteAdapter holds AudioGraphOwner; await owner.activeRouteSnapshot()"
      pattern: "AudioGraphRouteAdapter"
    - from: "MCPRuntimeWiringTests"
      to: "all four tools"
      via: "registersFourSelfKnowledgeTools test asserts each registered with requiresConfirmation:false"
      pattern: "registersFourSelfKnowledgeTools"
---

<objective>
Ship the four read-only MCP self-knowledge tools (`list_audio_devices`, `get_active_audio_route`, `get_self_state`, `list_camera_devices`) following the in-process adapter pattern from Track D-2 (`InProcessMemoryAdapters.swift`, commit `707c45b`). Tools register in MCPRuntimeWiring with `requiresConfirmation: false` (D-10 — explicit, not defaulted) and are reachable from a live Claude turn.

Purpose: Make "I don't have the ability to detect your hardware directly" / "I don't actually have a microphone" wrong answers impossible by giving the model real hardware introspection tools. Closes SELF-01..04 / AC-01..04.

Output:
- Four `InProcessTool` types under `packages/MCP/Sources/MCP/InProcess/`
- App-side adapter file `App/MCP/InProcessSelfStateAdapters.swift` (CoreAudio + AVCaptureDevice + Bundle/runtime introspection)
- Additive `AudioGraphOwner.activeRouteSnapshot()` actor accessor (~5 LOC)
- Registration callsite in `MCPRuntimeWiring` (or `installAgent` — wherever owns live `AudioGraphOwner`/`VoiceController`/`LLMProvider` refs)
- Wave-0 unit tests + extended `MCPRuntimeWiringTests/registersFourSelfKnowledgeTools`
- Live-launch verification: relaunch app, send a turn that asks "what mic are you using?" or invoke tools via DevOverlay; confirm tool calls appear and outputs cross-validate against `system_profiler`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-self-awareness-diagnostics/10-SPEC.md
@.planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md
@.planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md
@.planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md
@.planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md
@CLAUDE.md
@App/MCP/InProcessMemoryAdapters.swift
@App/MCP/MCPRuntimeWiring.swift
@packages/MCP/Sources/MCP/InProcess/InProcessTool.swift
@packages/MCP/Sources/MCP/InProcess/InProcessToolRegistry.swift
@packages/MCP/Sources/MCP/InProcess/SearchMemoryTool.swift
@packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift

<interfaces>
<!-- Extracted contracts the executor must implement against. Do NOT explore the codebase to discover these. -->

InProcessTool (packages/MCP/Sources/MCP/InProcess/InProcessTool.swift:10-31):
```swift
public protocol InProcessTool: Sendable {
    var name: String { get }
    var schemaJSON: Data { get }
    var requiresConfirmation: Bool { get }   // D-10: MUST be `false` explicitly for all four
    func call(args: Data) async throws -> Data
}
```

InProcessToolRegistry (packages/MCP/Sources/MCP/InProcess/InProcessToolRegistry.swift:6-30):
```swift
public actor InProcessToolRegistry {
    public init() {}
    public func register(_ tool: any InProcessTool) async
    // Aggregated into MCPToolDispatcher chain — verify the path used by SearchMemoryTool in installMemory (Plan 07-06).
}
```

Track D-2 reference adapter pattern (App/MCP/InProcessMemoryAdapters.swift:1-56) — protocol seam keeps MCP unaware of subsystem internals; App-side adapters bridge module boundary.

AudioGraphOwner (packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift:32-58):
```swift
public actor AudioGraphOwner {
    public var currentVariant: AudioGraphVariant? { get }    // .aecOn | .aecOff | nil
    public func subscribe(capacityFrames:) -> Subscription?  // Track B-7 fan-out
    // ADD (Plan 10-01): public func activeRouteSnapshot() -> ActiveRouteSnapshot?
    //   Reads from internal probed-format state populated at AudioGraph build time.
    //   Returns (inputUID, inputName, outputUID, outputName, sampleRate, channels).
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Create five test stubs (RED) for the four self-knowledge tools + registration assertion</name>
  <read_first>
    - packages/MCP/Tests/MCPTests/SearchMemoryToolTests.swift (sibling unit-test pattern — verbatim shape)
    - packages/MCP/Tests/MCPTests/MCPRuntimeWiringTests.swift (existing — extend with one new case)
    - .planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md (File Classification table — five W0 rows under Plan 10-01)
    - .planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md (Per-Task Verification Map — SELF-01..04 rows)
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 477-635 — Examples 1-4)
  </read_first>
  <behavior>
    - ListAudioDevicesToolTests: a `Stub`-style dispatcher returns canned `[AudioDeviceEntry]`; tool encodes `{ "devices": [...] }` JSON.
    - GetActiveAudioRouteToolTests: a `Stub`-style dispatcher returns a fixed `ActiveAudioRoute`; tool round-trips JSON.
    - GetSelfStateToolTests: a `Stub`-style dispatcher returns canned `SelfState` with all fields populated; tool round-trips JSON.
    - ListCameraDevicesToolTests: a `Stub`-style dispatcher returns one `CameraDeviceEntry` for the FaceTime camera; tool round-trips JSON.
    - MCPRuntimeWiringTests/registersFourSelfKnowledgeTools: builds the runtime, asserts the four tool names appear in the registry AND each has `requiresConfirmation == false`.
    - All five test files MUST fail (RED) before any production code lands. Test names use the Mock/Stub/Fake taxonomy from CLAUDE.md (D-31): the dispatcher doubles are `Stub*` (return canned data, do not record calls).
  </behavior>
  <action>
    Create five test files. Use Swift Testing `@Test` macros (matches sibling tests in `packages/MCP/Tests/MCPTests/SearchMemoryToolTests.swift`). Names follow D-31:

    1. `packages/MCP/Tests/MCPTests/ListAudioDevicesToolTests.swift` — declares `struct StubAudioDeviceListing: AudioDeviceListing { let entries: [AudioDeviceEntry]; func listAudioDevices() async throws -> [AudioDeviceEntry] { entries } }`. One `@Test` named `encodesDevicesArrayInResponseJSON`.
    2. `packages/MCP/Tests/MCPTests/GetActiveAudioRouteToolTests.swift` — `struct StubActiveAudioRouteDispatching: ActiveAudioRouteDispatching { ... }`. One `@Test` named `encodesActiveRoutePayload`.
    3. `packages/MCP/Tests/MCPTests/GetSelfStateToolTests.swift` — `struct StubSelfStateDispatching: SelfStateDispatching { ... }`. One `@Test` named `populatesAllSelfStateFields` asserting all 11 fields from D-11 (app_name, app_version, build_mode, pid, uptime_seconds, llm_provider, llm_model, tts_tier, stt_backend, wake_word_muted, voice_loop_state) are non-nil/non-default.
    4. `packages/MCP/Tests/MCPTests/ListCameraDevicesToolTests.swift` — `struct StubCameraDeviceListing: CameraDeviceListing { ... }`. One `@Test` named `encodesCameraEntriesArrayInResponseJSON`.
    5. EXTEND `packages/MCP/Tests/MCPTests/MCPRuntimeWiringTests.swift` with `@Test func registersFourSelfKnowledgeTools()` that asserts: `await registry.tools().map(\.name).sorted() contains [list_audio_devices, get_active_audio_route, get_self_state, list_camera_devices]`, AND `for each tool in those four, tool.requiresConfirmation == false`.

    Each test imports the (not-yet-existing) protocol+tool types — this MUST cause compile errors (RED). Per Wave 0 contract, this is intentional. Verify with `swift test --package-path packages/MCP` and observe compile failure citing missing types.

    Per RESEARCH §Pattern 1 + Pattern §Verification: test doubles named `Stub*` because they return canned data without recording calls (D-31).
  </action>
  <verify>
    <automated>cd /Users/james.maes/Git.Local/Kof22/Jarvis &amp;&amp; swift test --package-path packages/MCP --filter MCPTests 2>&amp;1 | grep -E "(error:|cannot find type)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/MCP/Tests/MCPTests/ListAudioDevicesToolTests.swift` exists and contains `@Test` and `StubAudioDeviceListing`
    - File `packages/MCP/Tests/MCPTests/GetActiveAudioRouteToolTests.swift` exists and contains `@Test` and `StubActiveAudioRouteDispatching`
    - File `packages/MCP/Tests/MCPTests/GetSelfStateToolTests.swift` exists and contains `@Test` and asserts on `app_version`, `pid`, `uptime_seconds`, `llm_provider`, `llm_model`, `voice_loop_state`
    - File `packages/MCP/Tests/MCPTests/ListCameraDevicesToolTests.swift` exists and contains `@Test` and `StubCameraDeviceListing`
    - `grep -c "registersFourSelfKnowledgeTools" packages/MCP/Tests/MCPTests/MCPRuntimeWiringTests.swift` returns ≥ 1
    - `swift test --package-path packages/MCP` produces compile errors mentioning at least one of: `ListAudioDevicesTool`, `AudioDeviceListing`, `GetActiveAudioRouteTool`, `GetSelfStateTool`, `ListCameraDevicesTool`
    - No `Mock` or `Fake` prefix appears on the new dispatcher doubles (`grep -E "struct (Mock|Fake)" packages/MCP/Tests/MCPTests/{ListAudioDevices,GetActiveAudioRoute,GetSelfState,ListCameraDevices}ToolTests.swift` returns empty)
  </acceptance_criteria>
  <done>Five test files in place, all failing to compile because the production types don't exist yet. RED phase complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement four InProcessTool types + dispatcher protocols (GREEN — Wave 0 tests pass)</name>
  <read_first>
    - packages/MCP/Sources/MCP/InProcess/SearchMemoryTool.swift (lines 25-66 — verbatim skeleton to swap names)
    - packages/MCP/Sources/MCP/InProcess/InProcessTool.swift (protocol contract)
    - .planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md (Pattern Assignments § Plan 10-01 first subsection — verbatim skeleton at line 75-92)
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 477-635 — Examples 1-4 for adapter bodies)
  </read_first>
  <action>
    Create four files under `packages/MCP/Sources/MCP/InProcess/`. Each follows the verbatim skeleton from PATTERNS.md lines 75-92 (D-10 explicit `requiresConfirmation = false`):

    1. **`ListAudioDevicesTool.swift`** —
       - Declare `public protocol AudioDeviceListing: Sendable { func listAudioDevices() async throws -> [AudioDeviceEntry] }`
       - Declare `public struct AudioDeviceEntry: Codable, Sendable { let name: String; let uid: String; let isDefault: Bool; let isActive: Bool; let isInput: Bool; let sampleRate: Double; let channels: Int }`
       - `public struct ListAudioDevicesTool: InProcessTool { public let name = "list_audio_devices"; public let requiresConfirmation = false; public var schemaJSON: Data { /* {"type":"object","properties":{},"required":[]} */ }; private let dispatcher: any AudioDeviceListing; init(dispatcher:); func call(args:) async throws -> Data { try JSONEncoder().encode(["devices": try await dispatcher.listAudioDevices()]) } }`

    2. **`GetActiveAudioRouteTool.swift`** —
       - `public protocol ActiveAudioRouteDispatching: Sendable { func getActiveAudioRoute() async throws -> ActiveAudioRoute? }`
       - `public struct ActiveAudioRoute: Codable, Sendable { let inputDeviceUID: String?; let inputDeviceName: String?; let outputDeviceUID: String?; let outputDeviceName: String?; let sampleRate: Double; let channels: Int; let variant: String /* "aecOn" | "aecOff" */ }`
       - Tool with `name = "get_active_audio_route"`, `requiresConfirmation = false`. `call` returns `JSONEncoder().encode(["route": try await dispatcher.getActiveAudioRoute()])` (encodes nil as `{"route": null}`).

    3. **`GetSelfStateTool.swift`** —
       - `public protocol SelfStateDispatching: Sendable { func getSelfState() async -> SelfState }`
       - `public struct SelfState: Codable, Sendable { let appName: String; let appVersion: String; let buildMode: String; let pid: Int; let uptimeSeconds: Int; let llmProvider: String; let llmModel: String; let ttsTier: String; let sttBackend: String; let wakeWordMuted: Bool; let voiceLoopState: String }`  // D-11 fields verbatim
       - Tool with `name = "get_self_state"`, `requiresConfirmation = false`. `call` returns `JSONEncoder().encode(await dispatcher.getSelfState())`.

    4. **`ListCameraDevicesTool.swift`** —
       - `public protocol CameraDeviceListing: Sendable { func listCameraDevices() async throws -> [CameraDeviceEntry] }`
       - `public struct CameraDeviceEntry: Codable, Sendable { let localizedName: String; let uniqueID: String; let isConnected: Bool; let position: String /* "front"|"back"|"external" */ }`
       - Tool with `name = "list_camera_devices"`, `requiresConfirmation = false`. `call` returns `JSONEncoder().encode(["cameras": try await dispatcher.listCameraDevices()])`.

    All four tools: schemaJSON is the trivial `{"type":"object","properties":{},"required":[]}` JSON literal (encode once into a `private static let _schema: Data`). Use `JSONEncoder()` with `.sortedKeys` for stable test fixtures. NO additional convenience initializers. NO retry logic. NO caching (D-09).

    Verify the four Wave 0 unit tests now pass: `swift test --package-path packages/MCP --filter ListAudioDevicesToolTests` (and the other three) — all GREEN.
  </action>
  <verify>
    <automated>cd /Users/james.maes/Git.Local/Kof22/Jarvis &amp;&amp; swift test --package-path packages/MCP --filter "ListAudioDevicesToolTests|GetActiveAudioRouteToolTests|GetSelfStateToolTests|ListCameraDevicesToolTests"</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under `packages/MCP/Sources/MCP/InProcess/`
    - `grep -c 'public let requiresConfirmation = false' packages/MCP/Sources/MCP/InProcess/{ListAudioDevices,GetActiveAudioRoute,GetSelfState,ListCameraDevices}Tool.swift` returns 4
    - `grep -c 'public let name = "list_audio_devices"' packages/MCP/Sources/MCP/InProcess/ListAudioDevicesTool.swift` returns 1
    - `grep -c 'public let name = "get_active_audio_route"' packages/MCP/Sources/MCP/InProcess/GetActiveAudioRouteTool.swift` returns 1
    - `grep -c 'public let name = "get_self_state"' packages/MCP/Sources/MCP/InProcess/GetSelfStateTool.swift` returns 1
    - `grep -c 'public let name = "list_camera_devices"' packages/MCP/Sources/MCP/InProcess/ListCameraDevicesTool.swift` returns 1
    - `swift test --package-path packages/MCP --filter "ListAudioDevicesToolTests|GetActiveAudioRouteToolTests|GetSelfStateToolTests|ListCameraDevicesToolTests"` exits 0
    - SelfState contains all 11 D-11 fields: `for f in appName appVersion buildMode pid uptimeSeconds llmProvider llmModel ttsTier sttBackend wakeWordMuted voiceLoopState; do grep -q "let $f:" packages/MCP/Sources/MCP/InProcess/GetSelfStateTool.swift; done` succeeds
  </acceptance_criteria>
  <done>Four `InProcessTool` types compile. All four Wave-0 tool tests pass GREEN. No caching. Each tool explicitly declares `requiresConfirmation = false` per D-10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement App-side adapters + AudioGraphOwner.activeRouteSnapshot() + register four tools (live runtime wiring)</name>
  <read_first>
    - App/MCP/InProcessMemoryAdapters.swift (lines 1-56 — Track D-2 reference adapter pattern, verbatim header comment)
    - App/MCP/MCPRuntimeWiring.swift (lines 1-130 — current registration block at lines 99-119)
    - App/AppDelegate.swift (search for `installAgent` and the AudioGraphOwner construction site — adapters need live refs)
    - App/Voice/VoiceOutputWiring.swift (lines 13-25 — TTSEngineActor tier-1 reference for the SelfStateAdapter ttsTier closure)
    - packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift (current actor body — additive `activeRouteSnapshot()` insertion site; search for "probed format" log line for field names)
    - packages/Voice/Sources/Voice/AudioGraph/AudioGraph.swift (search for "probed format" — extract sampleRate/channels field names that must mirror in ActiveRouteSnapshot)
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 477-635 — Examples 1-4 for adapter bodies; line 590 for activeRouteSnapshot() guidance)
    - .planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md (Plan 10-01 second + third subsections, lines 98-178)
    - build-devoverlay/SourcePackages/checkouts/argmax-oss-swift/Sources/WhisperKit/Core/Audio/AudioProcessor.swift (lines 818-879 — CoreAudio enumeration template; ~80 LOC)
  </read_first>
  <action>
    1. **Add `AudioGraphOwner.activeRouteSnapshot()`** (additive — `packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift`):
       Define `public struct ActiveRouteSnapshot: Sendable { public let inputUID: String?; public let inputName: String?; public let outputUID: String?; public let outputName: String?; public let sampleRate: Double; public let channels: Int }`. Add `public func activeRouteSnapshot() -> ActiveRouteSnapshot?` returning the values currently logged at the `AudioGraph: probed format sampleRate=X channels=Y` line. If the actor stores them on internal state, return them; if not, walk to the underlying graph and return what the probe captured. Method body ≤ 10 LOC. NO new state — read existing fields. NO TCC prompt — read-only.

    2. **Create `App/MCP/InProcessSelfStateAdapters.swift`** with header comment matching `InProcessMemoryAdapters.swift`'s boundary discipline ("These adapters live in App/ rather than packages/MCP/ because they cross the module boundary — JarvisMCP intentionally does NOT import Voice/AVFoundation/CoreAudio."). Imports: `Foundation`, `JarvisMCP`, `Voice`, `AVFoundation`, `CoreAudio`, `AudioToolbox`. Declare:

       - **`enum CoreAudioIntrospection`** with `static func snapshotDevices() throws -> [AudioDeviceEntry]` — full body per RESEARCH Example 1 (lines 477-524) using template from `WhisperKit/Core/Audio/AudioProcessor.swift:818-879`. Steps:
         (a) `kAudioHardwarePropertyDevices` → `[AudioDeviceID]`
         (b) For each ID, query `kAudioObjectPropertyName` (CFString), `kAudioDevicePropertyDeviceUID` (CFString), input streams via `kAudioDevicePropertyStreams` in `kAudioObjectPropertyScopeInput`, output streams in `Output`, `kAudioDevicePropertyNominalSampleRate`, channel count via stream config.
         (c) Resolve default in/out via `kAudioHardwarePropertyDefaultInputDevice` / `DefaultOutputDevice`.
         (d) `isActive = sampleRate > 0 && channels > 0`. Throw `CoreAudioError.size`/`.fetch` on `OSStatus != noErr`.
         Return `[AudioDeviceEntry]` (one entry per device per direction — input streams emit `isInput=true`, output streams emit `isInput=false`).

       - **`public struct CoreAudioDeviceListAdapter: AudioDeviceListing`** wrapping `CoreAudioIntrospection.snapshotDevices()`.

       - **`public actor AudioGraphRouteAdapter: ActiveAudioRouteDispatching`** — verbatim from RESEARCH Example 3 (lines 562-588). `init(owner: AudioGraphOwner)`, `getActiveAudioRoute() async -> ActiveAudioRoute?` calls `await owner.currentVariant`, `await owner.activeRouteSnapshot()`, returns `ActiveAudioRoute(... variant: String(describing: variant))`.

       - **`public struct SelfStateAdapter: SelfStateDispatching`** — verbatim from RESEARCH Example 4 (lines 594-635). Closure-injected `providerIdentity`, `voiceState`, `ttsTier`, `sttBackend`, `wakeWordMuted` to keep the adapter free of Voice/AgentCore imports of the actor types. `getSelfState()` reads `Bundle.main.infoDictionary` for `CFBundleName`/`CFBundleShortVersionString`, `#if DEBUG` for buildMode, `ProcessInfo.processInfo.processIdentifier` for pid, `Date().timeIntervalSince(launchInstant)` for uptime (NOT `ProcessInfo.systemUptime` — that's host uptime; D-11 specifies app uptime).

       - **`public struct AVCaptureDeviceListAdapter: CameraDeviceListing`** — verbatim from RESEARCH Example 2 (lines 528-558). `AVCaptureDevice.DiscoverySession(deviceTypes: [.builtInWideAngleCamera, .external, .continuityCamera], mediaType: .video, position: .unspecified)`. Read-only — does NOT trigger camera TCC prompt (verified RESEARCH line 560).

    3. **Wire into `MCPRuntimeWiring` / install path** (whichever owns the live `AudioGraphOwner` reference and the `LLMProvider` factory — confirm by reading `App/AppDelegate.swift` `installAgent`):
       After the existing helper-spawn registration block (`MCPRuntimeWiring.swift:97-119`), construct an `InProcessToolRegistry` and register all four tools:
       ```swift
       let inproc = InProcessToolRegistry()
       await inproc.register(ListAudioDevicesTool(dispatcher: CoreAudioDeviceListAdapter()))
       await inproc.register(GetActiveAudioRouteTool(dispatcher: AudioGraphRouteAdapter(owner: audioGraphOwner)))
       let selfState = SelfStateAdapter(
           bundle: .main,
           launchInstant: launchInstant,                      // captured at app start
           providerIdentity: { @Sendable in await providerFactory.identity() },  // adapt to real shape
           voiceState:       { @Sendable in await voiceController?.state.description },
           ttsTier:          { @Sendable in await ttsEngine?.activeTierName ?? "tier1" },
           sttBackend:       { @Sendable in await voiceController?.sttBackendName ?? "speechAnalyzer" },
           wakeWordMuted:    { @Sendable in await voiceController?.isWakeWordMuted ?? false }
       )
       await inproc.register(GetSelfStateTool(dispatcher: selfState))
       await inproc.register(ListCameraDevicesTool(dispatcher: AVCaptureDeviceListAdapter()))
       ```
       Thread `inproc` into the existing `MCPToolDispatcher` aggregation point — same path used by `SearchMemoryTool` in `installMemory` (Plan 07-06). Verify by reading the call chain from `installMemory` → MCPToolDispatcher.

       If a closure shape doesn't match the actual actor's API, ADAPT the closure expression (don't change the protocol). Document any closure-binding fallback in the file header.

       The `launchInstant: Date` ivar must be captured once in `AppDelegate.applicationDidFinishLaunching` (or the earliest install hook) and threaded through.

    4. **Make `MCPRuntimeWiringTests/registersFourSelfKnowledgeTools` GREEN.** If the test was written against the registry directly, that's already covered. If it's a build-time wiring test that needs an entry point, ensure the install function exposes the four tool names somewhere assertable (e.g., `MCPRuntimeWiring` returns the registered tool list, or a `tools()` helper on the InProcessToolRegistry).

    5. **No new boundary gates needed.** Verify all 18 existing gates remain green — especially `scripts/check-applescript-confirmation.sh` (must still see only `mcp-applescript` requesting confirmation; the new four tools must register `requiresConfirmation: false`).
  </action>
  <verify>
    <automated>cd /Users/james.maes/Git.Local/Kof22/Jarvis &amp;&amp; swift test --package-path packages/MCP --filter "MCPRuntimeWiringTests/registersFourSelfKnowledgeTools" &amp;&amp; swift test --package-path packages/Voice 2>&amp;1 | tail -5 &amp;&amp; bash scripts/check-app-builds.sh &amp;&amp; bash scripts/check-applescript-confirmation.sh &amp;&amp; bash scripts/check-no-leftover-stubs.sh &amp;&amp; bash scripts/check-install-order.sh &amp;&amp; bash scripts/check-no-null-voice-adapters.sh &amp;&amp; bash scripts/check-no-evaluate-javascript.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `App/MCP/InProcessSelfStateAdapters.swift` exists; first non-import non-comment line declares one of `CoreAudioIntrospection`, `CoreAudioDeviceListAdapter`, `AudioGraphRouteAdapter`, `SelfStateAdapter`, `AVCaptureDeviceListAdapter`
    - `grep -c "These adapters live in App/" App/MCP/InProcessSelfStateAdapters.swift` returns ≥ 1 (boundary header comment present)
    - `grep -c "AudioObjectGetPropertyData" App/MCP/InProcessSelfStateAdapters.swift` returns ≥ 2 (size + fetch calls; CoreAudio enumeration body present)
    - `grep -c "AVCaptureDevice.DiscoverySession" App/MCP/InProcessSelfStateAdapters.swift` returns ≥ 1
    - `grep -c "activeRouteSnapshot" packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift` returns ≥ 1
    - `grep -c "ActiveRouteSnapshot" packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift` returns ≥ 1
    - `grep -E "register\\(.*(ListAudioDevicesTool|GetActiveAudioRouteTool|GetSelfStateTool|ListCameraDevicesTool)" App/MCP/MCPRuntimeWiring.swift App/AppDelegate.swift` returns ≥ 4 distinct match lines
    - `swift test --package-path packages/MCP --filter "MCPRuntimeWiringTests/registersFourSelfKnowledgeTools"` exits 0
    - `swift test --package-path packages/Voice` exits 0 (regression check on AudioGraphOwner additive change)
    - `bash scripts/check-app-builds.sh` exits 0
    - All 18 boundary gates exit 0 (CLAUDE.md § Boundary gates list — run each)
    - `scripts/check-applescript-confirmation.sh` PASS — confirms only `mcp-applescript` retains `requiresConfirmation: true`; the four new tools register `false`
    - No `Null*Adapter` resurrected: `bash scripts/check-no-null-voice-adapters.sh` exits 0
  </acceptance_criteria>
  <done>Four self-knowledge tools register at install time against live runtime references. AudioGraphOwner exposes `activeRouteSnapshot()`. Boundary gates green. `MCPRuntimeWiringTests/registersFourSelfKnowledgeTools` PASS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live verification — relaunch + confirm tool calls + cross-validate against system_profiler</name>
  <read_first>
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 968-998 — Live-Launch Verification Format D-26 template; copy verbatim)
    - .planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md (Manual-Only Verifications table — `list_audio_devices` and `list_camera_devices` rows)
    - .planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md (D-26, D-27 — tests-green ≠ production-works; SUMMARY.md must include Live Verification or `/gsd-verify-phase 10` rejects)
  </read_first>
  <what-built>
    Four MCP tools registered against the live MCP runtime:
    - `list_audio_devices` — CoreAudio enumeration; returns inputs+outputs
    - `get_active_audio_route` — reads through AudioGraphOwner; non-nil while voice loop active
    - `get_self_state` — Bundle + ProcessInfo + closure-injected runtime probes
    - `list_camera_devices` — AVCaptureDevice.DiscoverySession enumeration
    Wave-0 unit tests green; MCPRuntimeWiring registration assertion green; all 18 boundary gates green.
  </what-built>
  <how-to-verify>
    Per D-26 template — copy the section verbatim into `.planning/phases/10-self-awareness-diagnostics/10-01-SUMMARY.md` with values filled in.

    **Plan 10-01 does NOT touch hardware capture surfaces directly** (CoreAudio + AVCaptureDevice.DiscoverySession enumeration are read-only metadata queries that do not trip TCC). Per D-28 guidance, plans 10-01 and 10-02 do not strictly require `tccutil reset`. Skip the reset step but include the line "Reset: N/A — Plan 10-01 reads CoreAudio + AVCaptureDevice metadata only; no hardware capture session opened" in the SUMMARY's Reset block.

    Pre-checks:
    1. `cd /Users/james.maes/Git.Local/Kof22/Jarvis && bash scripts/check-app-builds.sh` PASS
    2. All 18 boundary gates green (run each script in `scripts/check-*.sh` per CLAUDE.md § Boundary gates)
    3. `swift test --package-path packages/MCP` PASS

    Relaunch:
    - `bash scripts/check-app-builds.sh && open build/Build/Products/Debug/Jarvis.app`
    - Capture build artifact path + commit short-sha at `git rev-parse --short HEAD`

    Live verification (one of two paths — pick whichever is wired):

    Path A (preferred — if DevOverlay is already populated, e.g., post-Plan 10-03):
    - Open DevOverlay
    - Submit a text turn: "What microphone are you using right now?"
    - Observe the model invokes `get_active_audio_route` (visible in last-5 tool calls list)
    - Capture the tool result and the model's spoken/written answer
    - Diff the tool result's `inputDeviceName` + `inputDeviceUID` against `system_profiler SPAudioDataType | head -100`

    Path B (fallback — if DevOverlay is not yet populated, this plan ships before 10-03):
    - Use the MCP debug shell or invoke the tool directly via the agent test harness in DEBUG (search for `MCPToolDispatcher.dispatch` callsite in tests for an entry point), OR
    - Use a short integration test added under `packages/MCP/Tests/MCPTests/` that constructs the full registry and dispatches each of the four tools, asserts non-empty output. Document this in the SUMMARY's "Observed" block.

    Cross-validation:
    - For `list_audio_devices`: every `uid` in the tool output must have a matching `Audio ID` line in `system_profiler SPAudioDataType`
    - For `list_camera_devices`: every `uniqueID` in the tool output must appear in `system_profiler SPCameraDataType`
    - For `get_self_state`: `appVersion` matches `Bundle.main.infoDictionary[CFBundleShortVersionString]`; `pid` matches a running `Jarvis` PID via `ps -ax | grep Jarvis`; `llmModel` is `claude-opus-4-7` (per CLAUDE.md `claude-opus-4-7` lock)
    - For `get_active_audio_route`: `sampleRate` and `channels` match the most recent `AudioGraph: probed format sampleRate=X channels=Y` log line in `log stream --predicate 'subsystem == "com.koftwentytwo.jarvis"' --style compact`

    Write the populated `## Live Verification` section into `.planning/phases/10-self-awareness-diagnostics/10-01-SUMMARY.md`. The block MUST contain:
    - `## Live Verification (D-26 / SPEC AC-13)` header
    - Pre-checks block (3 items checked)
    - Reset block (N/A note as above)
    - Relaunch block with build artifact + commit short-sha
    - Observed block with at least 4 timestamped log lines OR (Path B) the integration-test exit codes + tool outputs
    - Acceptance Match block: `AC-01: PASS — <evidence>`, `AC-02: PASS — <evidence>`, `AC-03: PASS — <evidence>`, `AC-04: PASS — <evidence>`
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/10-self-awareness-diagnostics/10-01-SUMMARY.md` contains the heading `## Live Verification (D-26 / SPEC AC-13)`
    - SUMMARY contains `AC-01: PASS` AND `AC-02: PASS` AND `AC-03: PASS` AND `AC-04: PASS`
    - SUMMARY references at least one `system_profiler` output line for cross-validation
    - SUMMARY includes a build artifact path + commit short-sha
    - All 18 boundary gates exit 0 at the commit-of-record (re-run after writing SUMMARY)
  </acceptance_criteria>
  <resume-signal>Type "approved" once the Live Verification section is populated in 10-01-SUMMARY.md and the four ACs show PASS, or describe failures (e.g., "list_audio_devices returned no UIDs; suspect kAudioDevicePropertyDeviceUID query body has wrong scope").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model → MCP tool dispatcher | Untrusted JSON args from the LLM cross into Swift; tool input is parsed via `JSONDecoder` against `Codable` types — invalid shapes throw and become `tool_result` errors. |
| MCP tool → CoreAudio / AVFoundation | Tool calls into Apple system APIs; outputs are framework-validated. |
| MCP tool → AudioGraphOwner actor | Reads through actor isolation boundary; no shared mutable state in the tool body (D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-MCP-01 | Tampering / Elevation of Privilege | Self-knowledge tool registration | mitigate | All four tools register with `requiresConfirmation: false` EXPLICITLY (D-10). MCPRuntimeWiringTests/registersFourSelfKnowledgeTools asserts each one. `scripts/check-applescript-confirmation.sh` boundary gate continues to enforce that `mcp-applescript` is the only tool with `true`. Read-only tools must not silently inherit confirmation default. |
| T-10-SELF-01 | Information Disclosure | `get_self_state` payload | accept | All fields are non-sensitive runtime metadata: bundle name/version, pid, uptime, model name, voice loop state. No API keys, no transcript content, no PII. The model already has these via system prompt context. Risk accepted; no mitigation needed. |
| T-10-SELF-02 | Information Disclosure | `list_audio_devices` UIDs | accept | Device UIDs are user-scoped CoreAudio identifiers (e.g., `BuiltInMicrophoneDevice`); not secrets. `system_profiler SPAudioDataType` exposes the same data without elevated permissions. Risk accepted. |
| T-10-CAM-01 | Information Disclosure | `list_camera_devices` does NOT trip TCC | mitigate | `AVCaptureDevice.DiscoverySession.devices` enumeration is metadata-only and does not require Camera TCC (verified RESEARCH §Example 2 line 560). The tool returns `isConnected: true` even before TCC grant; actual capture would still fail. This is intentional — model can know the camera exists before TCC fires. |
| T-10-AUDIO-INTROSPECTION-01 | Denial of Service | CoreAudio query latency on slow USB devices | accept | `AudioObjectGetPropertyData` is local-only, microsecond-scale. No remote calls. No caching (D-09) — freshness > performance. Risk accepted. |
</threat_model>

<verification>
- All four Wave-0 tool unit tests PASS
- `MCPRuntimeWiringTests/registersFourSelfKnowledgeTools` PASS (asserts four names registered + each `requiresConfirmation == false`)
- `swift test --package-path packages/Voice` PASS (regression check after additive `activeRouteSnapshot()` accessor)
- `bash scripts/check-app-builds.sh` PASS
- All 18 boundary gates exit 0 (CLAUDE.md § Boundary gates)
- `.planning/phases/10-self-awareness-diagnostics/10-01-SUMMARY.md` contains a populated `## Live Verification` section with PASS evidence for AC-01..04 (per D-26 / SPEC AC-13)
</verification>

<success_criteria>
- `list_audio_devices`, `get_active_audio_route`, `get_self_state`, `list_camera_devices` are registered, callable from the live agent loop, and return populated payloads cross-validated against `system_profiler`.
- `AudioGraphOwner` exposes `activeRouteSnapshot()` (additive ≤10 LOC) without disturbing existing voice DAG behavior.
- 18 boundary gates green; no `Null*Adapter` resurrection; AppleScript confirmation gate semantics intact.
- Live Verification block in SUMMARY.md establishes the new procedural gate (D-26/27).
</success_criteria>

<output>
After completion, create `.planning/phases/10-self-awareness-diagnostics/10-01-SUMMARY.md` with:
- Standard summary template (objective, work delivered, files touched, tests added/changed, gates run)
- A `## Live Verification (D-26 / SPEC AC-13)` section per the canonical template at `10-RESEARCH.md` lines 968-998 with AC-01..04 marked PASS and evidence inline
</output>
