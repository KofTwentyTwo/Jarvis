---
phase: 10-self-awareness-diagnostics
plan: 04
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - App/MenuBar/MenuBarContextMenu.swift
  - App/MenuBar/MenuBarIconControllerTests.swift
  - App/AppDelegate.swift
  - .planning/phases/10-self-awareness-diagnostics/10-04-SUMMARY.md
autonomous: false
requirements: [DIAG-03, DIAG-04]
decisions_covered: [D-01, D-02, D-03, D-04, D-17, D-18, D-19, D-26, D-27, D-28, D-29, D-30, D-31]
must_haves:
  truths:
    - "Clicking the new 'Diagnostics → Audio Loopback Test (2s)' menu item records 2 seconds from the active mic via AudioGraphOwner.subscribe() and plays it back through AVAudioEngine within 1s of the capture window ending"
    - "Clicking the new 'Diagnostics → Speak Test Phrase' menu item synthesizes 'Jarvis is online' via TTSEngineActor tier-1 (AVSpeechSynthesizer) audibly within 2s"
    - "Audio loopback uses the existing BufferBroadcaster fan-out from Track B-7 (D-18) — no new audio tap, no new ring buffer (AP-10)"
    - "TTS playback uses TTSEngineActor.synthesize with tier: .tier1 (D-19) — no tier-2 path"
    - "Neither diagnostic touches the orchestrator's submit / cancelAndSubmit path (VOICE-14 / AP-08); they are pure I/O"
    - "Both diagnostics live under a 'Diagnostics' submenu in MenuBarContextMenu (D-17) — debug UI off the always-on HUD"
    - "All 18 boundary gates green at HEAD before plan SUMMARY.md commits (CLAUDE.md § Boundary gates)"
    - "bash scripts/check-app-builds.sh PASS at HEAD before plan SUMMARY.md commits"
    - "Live Verification section populated in SUMMARY.md per RESEARCH.md § Live-Launch Verification Format (D-26)"
    - "Test names follow Mock/Stub/Fake taxonomy from CLAUDE.md § Test naming conventions (D-31)"
    - "Plan structure honors D-01 (serial, no parallel-agent fan-out), D-02 (5 plans in foundation→UI order — diagnostic menu items are plan 4 of 5), D-03 (5 plans not 4 or 6), D-04 (every plan ends with Live Verification SUMMARY section)"
    - "Live-launch verification gate honors D-27 (tests-green is necessary but not sufficient — audio loopback and TTS playback are inherently audible/inaudible), D-28 (tccutil reset Microphone com.koftwentytwo.jarvis before audio loopback verification)"
    - "Boundary-gate discipline honors D-29 (18 existing boundary gates stay green; AudioGraph wiring change must not regress check-no-null-voice-adapters.sh), D-30 (no new boundary gate required)"
  artifacts:
    - path: "App/MenuBar/MenuBarContextMenu.swift"
      provides: "Diagnostics submenu with two NSMenuItems built via existing makeItem(title:action:) closure-trampoline pattern"
      contains: "Diagnostics"
    - path: "App/AppDelegate.swift"
      provides: "audioLoopbackAction() + ttsPlaybackAction() closures wired into MenuBarContextMenu.build(); both run on Task.detached and capture self weakly"
      contains: "audioLoopbackAction"
    - path: "App/MenuBar/MenuBarIconControllerTests.swift"
      provides: "Updated test parameter list threading audioLoopbackAction + ttsPlaybackAction closures (existing devOverlayToggleAction-style wiring)"
      contains: "audioLoopbackAction"
  key_links:
    - from: "audioLoopbackAction"
      to: "AudioGraphOwner.subscribe(capacityFrames:)"
      via: "Track B-7 BufferBroadcaster fan-out — reuses existing tap, doesn't disturb wake-word DAG (D-18 / AP-10)"
      pattern: "owner\\.subscribe\\(capacityFrames:"
    - from: "audioLoopbackAction playback"
      to: "AVAudioEngine + AVAudioPlayerNode (NEW separate engine)"
      via: "Plays through mainMixerNode; does NOT touch AudioGraphOwner's engine (RESEARCH Example 7 lines 752-769)"
      pattern: "AVAudioPlayerNode|AVAudioEngine"
    - from: "ttsPlaybackAction"
      to: "TTSEngineActor.synthesize(_:tier:voice:)"
      via: "tier: .tier1 (D-19); fixed phrase 'Jarvis is online'"
      pattern: "tier: \\.tier1|\"Jarvis is online\""
---

<objective>
Add two menu-bar diagnostic items under a new "Diagnostics" submenu — Audio Loopback Test (DIAG-03) and Speak Test Phrase (DIAG-04) — that prove mic input and speaker output work end-to-end without depending on wake-word, STT, LLM, or any inference. Pure I/O verification, exactly what the 2026-05-05/06 silent-mic + mute-TTS class of failures needed.

Purpose: Closes "no way to verify the speaker route works without depending on a complete agent turn" (SPEC line 73-76) and "no way to verify mic input → speaker output works end-to-end without depending on wake-word + STT + LLM + TTS" (SPEC line 68-71). Closes DIAG-03 / DIAG-04 / AC-09 / AC-10.

Output:
- `App/MenuBar/MenuBarContextMenu.swift` extended with a "Diagnostics" submenu (per D-17 — keeps debug UI off the HUD)
- `App/AppDelegate.swift` gains `audioLoopbackAction()` and `ttsPlaybackAction()` private methods; `MenuBarContextMenu.build(...)` call site threads them as new closure parameters
- `App/MenuBar/MenuBarIconControllerTests.swift` updated to thread the two new closures into the existing builder-test signature (search for `devOverlayToggleAction:` to find the threading sites; lines 14, 35, 50 per PATTERNS line 353)
- Live verification: relaunch app, click each menu item, confirm audible playback (loopback + speak)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-self-awareness-diagnostics/10-SPEC.md
@.planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md
@.planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md
@.planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md
@.planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md
@CLAUDE.md
@.planning/phases/10-self-awareness-diagnostics/10-03-SUMMARY.md
@App/MenuBar/MenuBarContextMenu.swift
@App/Voice/VoiceOutputWiring.swift
@packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift
@packages/Voice/Sources/Voice/TTS/TTSEngineActor.swift

<interfaces>
MenuBarContextMenu existing API (App/MenuBar/MenuBarContextMenu.swift):
- public static func build(title: String, devOverlayToggleAction: @escaping () -> Void, /* ... existing parameters ... */) -> NSMenu
- private static func makeItem(title: String, action: @escaping () -> Void) -> NSMenuItem  — closure-trampoline with `objc_setAssociatedObject` retain dance (NSMenuItem.target is weak; load-bearing pattern at lines 48-60)

AudioGraphOwner.subscribe (Track B-7 fan-out — packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift):
- public func subscribe(capacityFrames: Int) async -> Subscription?
- Subscription.ringBuffer.readMono16k(into: UnsafeMutableBufferPointer<Float>) -> Int

TTSEngineActor (packages/Voice/Sources/Voice/TTS/TTSEngineActor.swift:91-119, 192):
- public func synthesize(_ text: String, tier: TTSTier, voice: String) async throws
- Already yields .started / .firstAudio(at:) / .finished on its event stream
- Self-cancels on subsequent calls (lines 92-97) — diagnostic does not need to await events

NSMenuItem closure-trampoline pattern (App/MenuBar/MenuBarContextMenu.swift:48-60):
- objc_setAssociatedObject retain is load-bearing — NSMenuItem.target is weak; without the associated object the closure target deallocates and the menu item silently no-ops
- Reuse makeItem(title:action:) — do NOT inline new menu items
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Diagnostics submenu in MenuBarContextMenu + thread closures through; add audioLoopbackAction + ttsPlaybackAction in AppDelegate</name>
  <read_first>
    - App/MenuBar/MenuBarContextMenu.swift lines 1-80 (entire file — small; understand build() structure + makeItem closure-trampoline at 48-60)
    - App/MenuBar/MenuBarIconControllerTests.swift lines 1-60 (existing build() invocation sites — search for `devOverlayToggleAction:`; lines 14, 35, 50 per PATTERNS line 353)
    - App/Voice/VoiceOutputWiring.swift lines 13-25 (TTSEngineActor tier-1 constructor — confirms how tier-1 is wired today; the diagnostic uses the same engine)
    - packages/Voice/Sources/Voice/AudioGraph/AudioGraphOwner.swift (Subscription type + readMono16k method — confirm the interface exposed by Track B-7 fan-out)
    - packages/Voice/Sources/Voice/TTS/TTSEngineActor.swift lines 91-119, 192 (synthesize signature; cancel semantics)
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 717-793 — Examples 7 and 8 verbatim)
    - .planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md (Plan 10-04 subsections, lines 313-405)
    - App/AppDelegate.swift (locate the existing menu build call site; locate the AudioGraphOwner ivar and TTSEngine ivar)
  </read_first>
  <action>
    **A. Extend `App/MenuBar/MenuBarContextMenu.swift`** — add two parameters to `build(...)` and a Diagnostics submenu using the existing `makeItem(title:action:)` (D-17 / RESEARCH Example pattern):

    1. Add to `build(...)` signature:
       ```
       audioLoopbackAction: @escaping () -> Void,
       ttsPlaybackAction: @escaping () -> Void,
       ```
       Place these adjacent to the existing `devOverlayToggleAction:` (or wherever its closure cluster is — keep stylistic consistency).

    2. Inside `build(...)`, before the "Quit Jarvis" separator, insert (verbatim from PATTERNS lines 343-350):
       ```
       let diagnostics = NSMenuItem(title: "Diagnostics", action: nil, keyEquivalent: "")
       let diagSub = NSMenu(title: "Diagnostics")
       diagSub.addItem(makeItem(title: "Audio Loopback Test (2s)", action: audioLoopbackAction))
       diagSub.addItem(makeItem(title: "Speak Test Phrase",         action: ttsPlaybackAction))
       diagnostics.submenu = diagSub
       menu.addItem(NSMenuItem.separator())
       menu.addItem(diagnostics)
       ```
       MUST use `makeItem(title:action:)` — do NOT inline new `NSMenuItem`s without the closure-trampoline retain dance (the `objc_setAssociatedObject` pattern at lines 48-60 is load-bearing — NSMenuItem.target is weak; failure to retain causes silent menu-item no-op).

    **B. Add closures in `App/AppDelegate.swift`** — verbatim from RESEARCH Examples 7 and 8 (lines 720-769 and 776-792):

    1. `audioLoopbackAction()` — Task.detached, weak self capture, guards on `audioGraphOwner` and `subscribe(capacityFrames: 16_000 * 3)`. Captures 2 seconds (32,000 samples at 16 kHz mono) into a `[Float]` via `sub.ringBuffer.readMono16k(into:)` in a 1280-frame scratch buffer. Plays back via a NEW `AVAudioEngine` instance (NOT AudioGraphOwner's engine) using `AVAudioPlayerNode` + `AVAudioPCMBuffer`. Verbatim implementation per RESEARCH Example 7 lines 720-769.
       D-18 / AP-10 critical: MUST use `owner.subscribe(capacityFrames:)`. MUST NOT spawn a new audio tap. MUST NOT touch AudioGraphOwner's engine for playback.
       Include `private static func playMono16k(_ samples: [Float]) async` per RESEARCH Example 7 lines 752-769.

    2. `ttsPlaybackAction()` — Task.detached, weak self capture, guards on `ttsEngine`. Calls:
       ```
       try await engine.synthesize("Jarvis is online", tier: .tier1, voice: AVSpeechSynthesisVoice.currentLanguageCode())
       ```
       D-19 — fixed phrase "Jarvis is online", tier-1 only (no tier-2 path). Cancellation expected if user clicks again mid-speech (TTSEngineActor self-cancels per lines 92-97). Catch and swallow `error` silently.

    3. Wire both closures into the menu build call site. Find the existing `MenuBarContextMenu.build(...)` invocation in AppDelegate (the menu bar setup runs in `installMenuBar` or similar) and add `audioLoopbackAction: { [weak self] in self?.audioLoopbackAction() }` and `ttsPlaybackAction: { [weak self] in self?.ttsPlaybackAction() }` to the call.

    **C. Update `App/MenuBar/MenuBarIconControllerTests.swift`** — thread the two new closures (typically as no-op `{}` closures or test-double `Mock`-style recorders) into the existing test invocations of `MenuBarContextMenu.build(...)` at the three sites (PATTERNS line 353 cites lines 14, 35, 50; verify by reading the file). If the tests use a `Mock`-style recorder for the existing devOverlayToggleAction, mirror that pattern with the right D-31 prefix; if they use no-op `{}`, mirror that.

    **D. Verify all boundary gates remain green.** Specifically:
    - `scripts/check-applescript-confirmation.sh` — unchanged (no MCP tool registration touched)
    - `scripts/check-no-leftover-stubs.sh` — unchanged
    - `scripts/check-no-evaluate-javascript.sh` — unchanged (menu items are AppKit, not webview)
    - `scripts/check-no-null-voice-adapters.sh` — unchanged
    - `scripts/check-app-builds.sh` — must still PASS

    **E. NO orchestrator interaction.** Per VOICE-14 / AP-08, neither diagnostic may call `orchestrator.submit(_:)` or `orchestrator.cancelAndSubmit(_:)`. Verify post-implementation: `grep -E "(submit|cancelAndSubmit)" App/AppDelegate.swift` should NOT show new occurrences inside `audioLoopbackAction` or `ttsPlaybackAction` bodies.
  </action>
  <verify>
    <automated>cd /Users/james.maes/Git.Local/Kof22/Jarvis &amp;&amp; bash scripts/check-app-builds.sh &amp;&amp; bash scripts/check-no-leftover-stubs.sh &amp;&amp; bash scripts/check-install-order.sh &amp;&amp; bash scripts/check-no-evaluate-javascript.sh &amp;&amp; bash scripts/check-no-null-voice-adapters.sh &amp;&amp; bash scripts/check-applescript-confirmation.sh &amp;&amp; bash scripts/check-presence-bus-no-tts-orchestrator.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Diagnostics" App/MenuBar/MenuBarContextMenu.swift` returns at least 1 (submenu title present)
    - `grep -c "Audio Loopback Test" App/MenuBar/MenuBarContextMenu.swift` returns 1
    - `grep -c "Speak Test Phrase" App/MenuBar/MenuBarContextMenu.swift` returns 1
    - `grep -c "audioLoopbackAction:" App/MenuBar/MenuBarContextMenu.swift` returns at least 1 (parameter declared)
    - `grep -c "ttsPlaybackAction:" App/MenuBar/MenuBarContextMenu.swift` returns at least 1 (parameter declared)
    - `grep -c "func audioLoopbackAction" App/AppDelegate.swift` returns 1
    - `grep -c "func ttsPlaybackAction" App/AppDelegate.swift` returns 1
    - `grep -c "playMono16k" App/AppDelegate.swift` returns at least 1
    - `grep -c "owner.subscribe(capacityFrames:" App/AppDelegate.swift` returns at least 1 (Track B-7 reuse — D-18)
    - `grep -c '"Jarvis is online"' App/AppDelegate.swift` returns 1 (D-19 fixed phrase)
    - `grep -c "tier: \\.tier1" App/AppDelegate.swift` returns at least 1 (D-19 tier-1 only)
    - `grep -c "audioLoopbackAction" App/MenuBar/MenuBarIconControllerTests.swift` returns at least 1 (test signature updated)
    - `grep -c "AVAudioPlayerNode" App/AppDelegate.swift` returns at least 1 (separate AVAudioEngine for playback per RESEARCH Example 7)
    - In `audioLoopbackAction` and `ttsPlaybackAction` bodies (extract via awk: `awk '/func (audioLoopbackAction|ttsPlaybackAction)/,/^    \}$/' App/AppDelegate.swift`), the substring `orchestrator.submit` and `cancelAndSubmit` MUST NOT appear (VOICE-14 / AP-08)
    - `bash scripts/check-app-builds.sh` exits 0
    - All 18 boundary gates exit 0
  </acceptance_criteria>
  <done>Diagnostics submenu wired; both closures use existing primitives (B-7 fan-out for loopback, TTSEngineActor tier-1 for speak); orchestrator untouched; boundary gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification — click each diagnostic, confirm audible playback</name>
  <read_first>
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 968-998 — Live-Launch Verification Format D-26 template)
    - .planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md (Manual-Only Verifications: Audio loopback round-trip, TTS playback audible at expected route)
    - .planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md (D-26, D-28 — tccutil reset MANDATORY for Plan 10-04 because both diagnostics touch hardware capture surfaces)
  </read_first>
  <what-built>
    - "Diagnostics" submenu under the menu bar item with two entries: "Audio Loopback Test (2s)" and "Speak Test Phrase"
    - audioLoopbackAction reuses AudioGraphOwner.subscribe (Track B-7 fan-out); plays back via separate AVAudioEngine
    - ttsPlaybackAction calls TTSEngineActor.synthesize tier-1 with fixed phrase "Jarvis is online"
    - Both diagnostics are pure I/O — no orchestrator touch, no inference path
  </what-built>
  <how-to-verify>
    Per D-26 template — copy the section verbatim into `.planning/phases/10-self-awareness-diagnostics/10-04-SUMMARY.md`. D-28 MANDATES tccutil reset for Plan 10-04 (both diagnostics touch hardware capture).

    Reset:
    - `tccutil reset Microphone com.koftwentytwo.jarvis`
    - `tccutil reset Camera com.koftwentytwo.jarvis` (camera not used here, but reset for safety per D-28 / RESEARCH AP-03)

    Pre-checks:
    1. `bash scripts/check-app-builds.sh` PASS
    2. All 18 boundary gates green
    3. Speakers + microphone connected and not in use by another app

    Relaunch:
    - `bash scripts/check-app-builds.sh && open build/Build/Products/Debug/Jarvis.app`
    - Capture build artifact path + commit short-sha at `git rev-parse --short HEAD`
    - On relaunch, click Allow on the mic permission prompt (verify the explicit-requestAccess path fires per RESEARCH AP-03 / b92d062). Document the prompt observation in the SUMMARY.

    Live verification — Test 1 (DIAG-03 / AC-09): Audio Loopback
    1. Click menu bar item → Diagnostics → "Audio Loopback Test (2s)".
    2. Within ~100ms, the recording window opens. Speak audibly during the 2-second window: "Hello, this is a loopback test."
    3. Within 1 second of the recording window ending, hear the recorded audio play back through the active speaker.
    4. Capture: timestamps from `log stream --predicate 'subsystem == "com.koftwentytwo.jarvis"' --style compact` (look for `audioLoopbackAction` start + `subscribe` + `playMono16k` end log lines if any), plus the human observation: "Heard the recorded loopback audio within X.Xs of the capture window ending — YES/NO."
    5. If silence: the mic permission isn't actually taking effect even if TCC says granted. Document the failure mode + the `tccd` log lines in the SUMMARY.

    Live verification — Test 2 (DIAG-04 / AC-10): TTS Playback
    1. Click menu bar item → Diagnostics → "Speak Test Phrase".
    2. Within 2 seconds, hear "Jarvis is online" spoken via AVSpeechSynthesizer through the active speaker.
    3. Capture: timestamps + log evidence + human observation: "Heard 'Jarvis is online' audibly within X.Xs of clicking — YES/NO."
    4. If silence: the TTS engine or speaker route is misconfigured even though no error appears in logs. Document.

    Bonus: click the loopback diagnostic a second time mid-playback to verify it doesn't crash (TTSEngineActor self-cancels; loopback's separate AVAudioEngine should also handle a re-entry gracefully — observe in logs).

    Write the populated `## Live Verification (D-26 / SPEC AC-13)` section into 10-04-SUMMARY.md. The block MUST contain:
    - Pre-checks block
    - Reset block (with tccutil reset commands run + observed prompts)
    - Relaunch block with build artifact + commit short-sha
    - Observed block: timestamped log lines + the verbatim human observations for each test
    - Acceptance Match block: `AC-09: PASS — heard loopback within Xs of capture end` AND `AC-10: PASS — heard "Jarvis is online" within Xs of click`
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/10-self-awareness-diagnostics/10-04-SUMMARY.md` contains heading `## Live Verification (D-26 / SPEC AC-13)`
    - SUMMARY contains both `AC-09: PASS` AND `AC-10: PASS`
    - SUMMARY records the tccutil reset commands and the observed mic permission prompt at relaunch
    - SUMMARY records a verbatim human observation for each diagnostic (audible-yes/no + measured latency)
    - SUMMARY records at least 4 timestamped log lines
    - All 18 boundary gates exit 0 at commit-of-record
  </acceptance_criteria>
  <resume-signal>Type "approved" once both AC-09 and AC-10 PASS audibly. If silent (mic delivers no audio OR TTS plays no sound), describe which diagnostic failed + the log evidence so the executor can iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Menu item action → AppDelegate closures | NSMenuItem target is weak; closure-trampoline pattern with `objc_setAssociatedObject` retain is load-bearing. New menu items use existing makeItem(title:action:) — no new trust seam. |
| audioLoopbackAction → AudioGraphOwner subscriber | Reuses Track B-7 BufferBroadcaster fan-out; back-pressure built into the bounded ring buffer. Doesn't disturb wake-word DAG (D-18). |
| ttsPlaybackAction → TTSEngineActor | Tier-1 AVSpeechSynthesizer is a system-trust boundary; output goes to the active speaker route. No barge-in / interrupt sequence required (D-19). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-AUDIO-01 | Tampering / Quality | Audio loopback diagnostic spawns a new tap that races wake-word DAG and corrupts VOICE-10 six-step teardown | mitigate | D-18 / AP-10: MUST use `AudioGraphOwner.subscribe(capacityFrames:)` (Track B-7 fan-out). Acceptance asserts the call is present. NEW AVAudioEngine is for PLAYBACK only — separate from AudioGraphOwner's engine (RESEARCH Example 7 lines 752-769). |
| T-10-DIAG-01 | Tampering / VOICE-14 violation | Diagnostic touches orchestrator.submit / cancelAndSubmit, breaking the single-call-site discipline | mitigate | AP-08: diagnostics are pure I/O. Acceptance grep on the function bodies asserts `orchestrator.submit` and `cancelAndSubmit` do NOT appear. Boundary gate `scripts/check-presence-bus-no-tts-orchestrator.sh` (or equivalent — verify in CLAUDE.md) provides additional defense. |
| T-10-MENU-01 | Denial of Service | NSMenuItem target weakly held → closure deallocates → menu item silently no-ops | mitigate | Use existing `makeItem(title:action:)` with the `objc_setAssociatedObject .OBJC_ASSOCIATION_RETAIN_NONATOMIC` retain. Acceptance asserts both new items go through `makeItem` (no inline NSMenuItem construction in MenuBarContextMenu.swift for the new items). |
| T-10-LOOPBACK-01 | Information Disclosure | Loopback records 2s of mic audio into an in-memory buffer that could leak via crash dump | accept | In-memory `[Float]` array; not written to disk; not logged (no transcript-to-OSLog issue — these are raw PCM samples not text). Diagnostic is one-shot; buffer deallocates after playback. Risk accepted. |
| T-10-TTS-01 | Tampering | TTS diagnostic activates while a real agent turn is mid-TTS | mitigate | TTSEngineActor self-cancels on subsequent calls (lines 92-97); the diagnostic awaits its own throwing call and swallows cancellation. Real turns will momentarily interrupt but won't crash. Documented in code comment. |
</threat_model>

<verification>
- `bash scripts/check-app-builds.sh` PASS
- All 18 boundary gates exit 0
- `audioLoopbackAction` body uses `AudioGraphOwner.subscribe(capacityFrames:)` (D-18 verified by grep)
- `audioLoopbackAction` and `ttsPlaybackAction` bodies do NOT call `orchestrator.submit` or `cancelAndSubmit` (VOICE-14 / AP-08 verified by grep)
- Both new menu items go through `makeItem(title:action:)` — no inline NSMenuItem constructions in the new diff
- `.planning/phases/10-self-awareness-diagnostics/10-04-SUMMARY.md` contains `## Live Verification` with AC-09 + AC-10 PASS, audible observations recorded
</verification>

<success_criteria>
- "Diagnostics" submenu visible and functional from the menu bar item
- Clicking "Audio Loopback Test (2s)" records 2 seconds and plays back audibly within 1s of capture-window end
- Clicking "Speak Test Phrase" speaks "Jarvis is online" via tier-1 TTS within 2s
- Diagnostics share zero code with the orchestrator submit path; all 18 boundary gates green
- The 2026-05-05 silent-mic class of bugs is now diagnosable from a single menu click without a full agent turn
</success_criteria>

<output>
After completion, create `.planning/phases/10-self-awareness-diagnostics/10-04-SUMMARY.md` with:
- Standard summary template
- A `## Live Verification (D-26 / SPEC AC-13)` section with AC-09 + AC-10 PASS, tccutil reset commands recorded, audible observations + measured latencies inline
</output>
