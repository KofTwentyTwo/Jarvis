---
phase: 10-self-awareness-diagnostics
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - App/AppDelegate.swift
  - packages/DevOverlay/Tests/DevOverlayTests/DevOverlayBroadcasterIntegrationTests.swift
  - .planning/phases/10-self-awareness-diagnostics/10-03-SUMMARY.md
  - .planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md
autonomous: false
requirements: [DIAG-02]
decisions_covered: [D-01, D-02, D-03, D-04, D-05, D-15, D-16, D-26, D-27, D-28, D-29, D-30, D-31]
must_haves:
  truths:
    - "Within 100ms of an LLM turn's .turnEnded event, the DevOverlay window's six fields populate: Provider, Model, Input tokens, Output tokens, Cache creation/read tokens (with hit %), Latency (ttfb + total)"
    - "The last-5 tool calls list shows the most recent tool calls with name + duration"
    - "The plan ships in TWO commits in this order (D-15/D-16): (1) read-only INVESTIGATION.md note explaining what was actually broken, (2) surgical wiring fix"
    - "DevOverlayBridge and DevOverlayViewModel are NOT refactored (D-16 / AP-13)"
    - "AppDelegate retains DevSnapshotEmitter and DevOverlayBridge as ivars to keep their subscriber tasks alive (RESEARCH Example 6 lifecycle note)"
    - "All 18 boundary gates green at HEAD before plan SUMMARY.md commits (CLAUDE.md § Boundary gates)"
    - "bash scripts/check-app-builds.sh PASS at HEAD before plan SUMMARY.md commits"
    - "Live Verification section populated in SUMMARY.md per RESEARCH.md § Live-Launch Verification Format (D-26)"
    - "Test names follow Mock/Stub/Fake taxonomy from CLAUDE.md § Test naming conventions (D-31)"
    - "Plan structure honors D-01 (serial, no parallel-agent fan-out), D-02 (5 plans in foundation→UI order — DevOverlay fix is plan 3 of 5), D-03 (5 plans not 4 or 6), D-04 (every plan ends with Live Verification SUMMARY section)"
    - "Live-launch verification gate honors D-27 (tests-green is necessary but not sufficient), D-28 (tccutil reset Microphone com.koftwentytwo.jarvis before relaunch — verification depends on a live turn that triggers the mic chain via DevOverlay tool-call observation)"
    - "Boundary-gate discipline honors D-29 (18 existing boundary gates stay green at every plan boundary), D-30 (no new boundary gate required)"
  artifacts:
    - path: ".planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md"
      provides: "Read-only investigation note (commit 1 of plan) explaining root cause: AppDelegate.swift:1325-1330 drains broadcaster events into a no-op for-await loop instead of forwarding to DevSnapshotEmitter; toggleDevOverlay() at 2020-2027 constructs window without bridging to the (unbuilt) emitter"
      min_lines: 30
    - path: "App/AppDelegate.swift"
      provides: "Surgical wiring fix per RESEARCH Example 6: construct DevSnapshotEmitter, replace no-op drain at 1325-1330 with `await devSnapshotEmitter.apply(event)`, attach bridge to emitter.output in toggleDevOverlay; new ivars devSnapshotEmitter and devOverlayBridge"
      contains: "devSnapshotEmitter"
    - path: "packages/DevOverlay/Tests/DevOverlayTests/DevOverlayBroadcasterIntegrationTests.swift"
      provides: "Salvaged from stash@{0} per D-05 — integration test fixture for the broadcaster→emitter→bridge→viewModel pipeline"
      min_lines: 100
  key_links:
    - from: "AppDelegate.installAgent broadcaster.subscribe(.devOverlay)"
      to: "DevSnapshotEmitter.apply(event)"
      via: "for-await loop forwards event (replaces no-op drain at lines 1325-1330)"
      pattern: "devSnapshotEmitter\\.apply"
    - from: "DevOverlayBridge"
      to: "DevSnapshotEmitter.output channel"
      via: "bridge.attach(channel: emitter.output) called in toggleDevOverlay()"
      pattern: "bridge\\.attach\\(channel:"
    - from: "AppDelegate ivars"
      to: "DevSnapshotEmitter + DevOverlayBridge"
      via: "strong references prevent subscriber-task deallocation (RESEARCH Example 6 lifecycle note)"
      pattern: "self\\.devSnapshotEmitter|self\\.devOverlayBridge"
---

<objective>
Make the existing DevOverlay window populate during turns. Two-step pattern (D-15/D-16, mandatory order): (1) read-only investigation note FIRST that explains what is actually broken, (2) surgical wiring fix SECOND. NO refactor of `DevOverlayBridge`, `DevOverlayViewModel`, or `DevSnapshotEmitter` — those are correct (AP-13). The bug is upstream wiring at two specific AppDelegate.swift sites.

Purpose: Closes the 2026-05-05 user-quoted failure mode "DevOverlay window opened but every field showed empty/zero state (`Provider: /`, `Input: 0 tok`, etc.)". Closes DIAG-02 / AC-08. Also the natural visualizer for Plan 10-02's live-launch verification (which is why 10-03 lands as Wave 3, depending on 10-02).

Output:
- Commit 1: `.planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md` documenting the two broken sites (no source changes in this commit).
- Commit 2: surgical AppDelegate.swift fix per RESEARCH Example 6 (lines 680-712) — construct `DevSnapshotEmitter`, replace no-op drain at 1325-1330 with `await devSnapshotEmitter.apply(event)`, attach bridge to emitter.output in `toggleDevOverlay()`, add ivars to retain.
- Commit 3 (or together with 2): salvage `packages/DevOverlay/Tests/DevOverlayTests/DevOverlayBroadcasterIntegrationTests.swift` from `stash@{0}` (verdict: SALVAGE per RESEARCH Stash Salvage Triage line 1009 / PATTERNS line 309).
- Live verification: relaunch, send a turn that calls `get_time` (or any tool from Plan 10-01), confirm DevOverlay populates within 100ms of `.turnEnded`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-self-awareness-diagnostics/10-SPEC.md
@.planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md
@.planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md
@.planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md
@.planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md
@CLAUDE.md
@.planning/phases/10-self-awareness-diagnostics/10-02-SUMMARY.md
@packages/AgentCore/Sources/AgentOrchestrator/DevSnapshotEmitter.swift
@packages/AgentCore/Sources/AgentOrchestrator/DevSnapshot.swift
@packages/DevOverlay/Sources/DevOverlay/DevOverlayBridge.swift
@packages/DevOverlay/Sources/DevOverlay/DevOverlayViewModel.swift
@packages/DevOverlay/Sources/DevOverlay/DevOverlayWindow.swift

<interfaces>
DevSnapshotEmitter (packages/AgentCore/Sources/AgentOrchestrator/DevSnapshotEmitter.swift:22-92):
```
public actor DevSnapshotEmitter {
    public init(provider: String, modelId: String, capacity: Int)
    public let output: BoundedAsyncChannel<DevSnapshot>   // `let`, safe to capture
    public func apply(_ event: BroadcasterEvent) async    // accumulates + flushes on turn end
}
```

DevOverlayBridge (packages/DevOverlay/Sources/DevOverlay/DevOverlayBridge.swift:17-52):
```
@MainActor public final class DevOverlayBridge {
    public init(viewModel: DevOverlayViewModel)
    public func attach(channel: BoundedAsyncChannel<DevSnapshot>)   // spawns subscriber Task
    // viewModel held weakly (line 19) — bridge MUST be retained externally
}
```

DevOverlayViewModel (packages/DevOverlay/Sources/DevOverlay/DevOverlayViewModel.swift:17-42):
- `private(set) var snapshot: DevSnapshot` — single-writer enforcement; do NOT reach into this directly (AP-14)

DevOverlayWindow (packages/DevOverlay/Sources/DevOverlay/DevOverlayWindow.swift:32-48):
- NSPanel + NSHostingView template; mirrors Voice Log pattern in Plan 10-05

Current broken sites (App/AppDelegate.swift):
- Lines 1325-1330: broadcaster.subscribe(.devOverlay) → no-op `for await _ in ... { if Task.isCancelled break }` drain
- Lines 2020-2027: toggleDevOverlay() constructs window + bridge but never calls bridge.attach(channel:) because the emitter doesn't exist yet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (commit 1 of 2 — D-15 mandatory): Author read-only INVESTIGATION.md root-cause note (no source changes)</name>
  <read_first>
    - App/AppDelegate.swift lines 1310-1340 (current no-op drain site)
    - App/AppDelegate.swift lines 2010-2040 (current toggleDevOverlay() site)
    - packages/AgentCore/Sources/AgentOrchestrator/DevSnapshotEmitter.swift (existing emitter — already correct, has apply + output)
    - packages/DevOverlay/Sources/DevOverlay/DevOverlayBridge.swift (already correct — has attach(channel:))
    - packages/DevOverlay/Sources/DevOverlay/DevOverlayViewModel.swift (already correct)
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 677-715 — Example 6 surgical fix; identifies the two broken sites)
    - .planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md (D-15, D-16, AP-13, AP-14, AP-16)
  </read_first>
  <action>
    Create `.planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md` with these sections (no source code changes in this commit per D-15 / AP-16):

    1. **Header:** "DIAG-02 root-cause investigation — Plan 10-03 commit 1 of 2 (D-15)".
    2. **Symptom (verbatim from SPEC line 64-66):** "DevOverlay window opens but all fields stay zero because the subscriber wiring is broken (or because no turns ever completed pre-d8983af)." Note that `d8983af` (stream:true fix) closed the second cause; the first cause remains and is what this plan addresses.
    3. **Components inspected (each marked CORRECT or BROKEN with file:line citations):**
       - DevSnapshotEmitter — CORRECT — has `apply(_:)` and `let output` (file:line)
       - DevOverlayBridge — CORRECT — `attach(channel:)` spawns subscriber Task; viewModel held weakly (line 19)
       - DevOverlayViewModel — CORRECT — `private(set) var snapshot` single-writer enforcement
       - DevOverlayWindow — CORRECT — NSPanel + NSHostingView template
       - AppDelegate.swift:1325-1330 — **BROKEN — no-op drain.** The broadcaster.subscribe(.devOverlay) loop discards events (`for await _ in ... { if Task.isCancelled break }`). Events never reach the emitter because the emitter is never constructed and never wired in.
       - AppDelegate.swift:2020-2027 — **BROKEN — bridge never attached.** toggleDevOverlay() constructs window + bridge but never calls bridge.attach(channel:) — there is no emitter for the bridge to attach to.
    4. **Root cause:** the emitter (`DevSnapshotEmitter`) is unconstructed in production. Both broken sites are upstream-wiring gaps, not bugs in the emitter / bridge / view-model. AP-13 / AP-14 explicitly forbid refactoring those types.
    5. **Surgical fix plan (commit 2):** mirror RESEARCH Example 6 (lines 680-712):
       - Construct `DevSnapshotEmitter(provider:modelId:capacity:)` inside `installAgent` after the broadcaster, store on `self.devSnapshotEmitter` ivar.
       - Replace lines 1325-1330 no-op `for await _ in ...` with `for await event in devSub.stream { if Task.isCancelled { break }; await devSnapshotEmitter.apply(event) }`.
       - In `toggleDevOverlay()`, after constructing window + bridge, call `bridge.attach(channel: emitter.output)` (capture under `Task { @MainActor in ... }` because `emitter.output` is on an actor — actually `output` is `let` so directly accessible; no actor hop required, but RESEARCH Example 6 wraps it in MainActor task for safety).
       - Add `self.devOverlayBridge = bridge` ivar so subscriber Task isn't deallocated.
    6. **Why no DevOverlayBridge/ViewModel refactor is needed (AP-13):** the bridge already has the contract `attach(channel:)` and the view-model already has `apply(_:)` single-writer. They were correct from Phase 4; the wiring just never landed.
    7. **Tests-green invariant (D-27):** the existing `DevSnapshotEmitterTests/applyTurnEndedFlushesAccumulator` already passes (per VALIDATION.md row); no test was failing pre-10-03 because the wiring gap is at the integration boundary, not in any unit-tested type. The salvaged `DevOverlayBroadcasterIntegrationTests.swift` from stash@{0} closes that integration gap.

    Commit this file alone. No source changes. Commit message: `docs(phase-10): plan 10-03 commit 1 — DIAG-02 root-cause investigation note (D-15)`.
  </action>
  <verify>
    <automated>cd /Users/james.maes/Git.Local/Kof22/Jarvis &amp;&amp; test -f .planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md &amp;&amp; grep -c "AppDelegate.swift:1325" .planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md &amp;&amp; grep -c "AppDelegate.swift:202" .planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md &amp;&amp; git status --porcelain | grep -v -E "(10-03-INVESTIGATION.md|10-03-PLAN.md)" | grep -E "^[ AM]" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md` exists
    - File contains "AppDelegate.swift:1325" (cites first broken site)
    - File contains "AppDelegate.swift:202" (cites second broken site — toggleDevOverlay)
    - File contains "DevSnapshotEmitter" + "BROKEN" + "CORRECT" labels
    - File contains a "Surgical fix plan" section explaining commit 2's intent
    - File length ≥ 30 lines
    - At commit time, no other files in `App/` or `packages/` are modified (D-15 / AP-16: investigation-only commit)
    - The verify command's final pipe count returns 0 (no other modified files staged for the investigation commit)
  </acceptance_criteria>
  <done>INVESTIGATION.md committed alone. No source changes. D-15 / AP-16 satisfied (investigation BEFORE fix).</done>
</task>

<task type="auto">
  <name>Task 2 (commit 2 — surgical fix per D-16): Wire DevSnapshotEmitter into AppDelegate; salvage integration test from stash</name>
  <read_first>
    - .planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md (just authored)
    - App/AppDelegate.swift lines 1300-1340 + 2010-2040 (the two broken sites + ivar declaration zone — typically near top of class)
    - packages/AgentCore/Sources/AgentOrchestrator/DevSnapshotEmitter.swift (verify init signature: provider, modelId, capacity)
    - packages/DevOverlay/Sources/DevOverlay/DevOverlayBridge.swift (verify attach(channel:) signature)
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 677-716 — Example 6 verbatim)
    - .planning/phases/10-self-awareness-diagnostics/10-PATTERNS.md (lines 240-306 — Plan 10-03 surgical fix subsections)
    - stash@{0} contents — `git stash show -p stash@{0} -- packages/DevOverlay/Tests/DevOverlayTests/DevOverlayBroadcasterIntegrationTests.swift` to inspect the integration test fixture
  </read_first>
  <action>
    Two coupled edits + one salvage. Commit them together as commit 2 of the plan.

    **Edit A — AppDelegate.swift:1325-1330 (replace no-op drain).**
    Read the surrounding 30 lines first. The current code is verbatim per PATTERNS lines 252-262:
    ```
    let devSub = await broadcaster.subscribe(priority: .devOverlay, capacity: 32)
    devOverlaySubscriberTask = Task {
        for await _ in devSub.stream {
            if Task.isCancelled { break }
        }
    }
    ```
    Replace with (verbatim from RESEARCH Example 6 lines 680-696 / PATTERNS lines 265-279):
    ```
    let devSnapshotEmitter = DevSnapshotEmitter(
        provider: "anthropic",          // TODO: plumb from providerFactory
        modelId: "claude-opus-4-7",
        capacity: 32                    // AGENT-10 four-seam compliance
    )
    self.devSnapshotEmitter = devSnapshotEmitter

    let devSub = await broadcaster.subscribe(priority: .devOverlay, capacity: 32)
    devOverlaySubscriberTask = Task {
        for await event in devSub.stream {
            if Task.isCancelled { break }
            await devSnapshotEmitter.apply(event)
        }
    }
    ```
    Add `var devSnapshotEmitter: DevSnapshotEmitter?` ivar near the existing `devOverlayWindow` / `devOverlaySubscriberTask` ivar declarations.

    **Edit B — AppDelegate.swift:2020-2027 (toggleDevOverlay).**
    Replace with (verbatim from RESEARCH Example 6 lines 698-713 / PATTERNS lines 287-303):
    ```
    private func toggleDevOverlay() {
        if devOverlayWindow == nil {
            let window = DevOverlayWindow()
            let bridge = DevOverlayBridge(viewModel: window.viewModel)
            if let emitter = devSnapshotEmitter {
                Task { @MainActor in
                    bridge.attach(channel: await emitter.output)
                }
            }
            self.devOverlayWindow = window
            self.devOverlayBridge = bridge
        }
        devOverlayWindow?.toggle()
    }
    ```
    Add `var devOverlayBridge: DevOverlayBridge?` ivar.

    **Note on emitter.output access:** `output` is declared `public let` on the actor's stored property. `let` properties on an actor are nonisolated and can be accessed without `await` — but the RESEARCH example wraps in `Task { @MainActor in ... }` for safety. Pick whichever the compiler accepts. If `await emitter.output` fails, drop the `await`.

    **Salvage C — DevOverlayBroadcasterIntegrationTests.swift.**
    Per D-05 / RESEARCH Stash Salvage Triage line 1009 / PATTERNS line 309 — verdict SALVAGE as-is. Use:
    ```
    git show 101a2aa1:packages/DevOverlay/Tests/DevOverlayTests/DevOverlayBroadcasterIntegrationTests.swift > packages/DevOverlay/Tests/DevOverlayTests/DevOverlayBroadcasterIntegrationTests.swift
    ```
    (101a2aa1 is the third parent of stash@{0} per RESEARCH line 1001 — holds untracked files.) If the file does not appear in that parent, fall back to `git stash show -p stash@{0} -- packages/DevOverlay/Tests/DevOverlayTests/DevOverlayBroadcasterIntegrationTests.swift` and apply the diff manually.

    Compile and run: `swift test --package-path packages/DevOverlay`. The integration test must compile and pass (or, if the salvaged file references types that have evolved since stash time, perform the minimum edits to make it compile — do NOT rewrite its assertions).

    Verify all test-double names follow D-31 (Mock = records, Stub = canned, Fake = realistic). Rename inline if the salvaged file uses the wrong taxonomy.

    Run all 18 boundary gates. None should regress.

    Commit message: `fix(phase-10): plan 10-03 commit 2 — wire DevSnapshotEmitter; salvage broadcaster integration test (DIAG-02)`.
  </action>
  <verify>
    <automated>cd /Users/james.maes/Git.Local/Kof22/Jarvis &amp;&amp; swift test --package-path packages/DevOverlay &amp;&amp; bash scripts/check-app-builds.sh &amp;&amp; bash scripts/check-no-leftover-stubs.sh &amp;&amp; bash scripts/check-install-order.sh &amp;&amp; bash scripts/check-no-evaluate-javascript.sh &amp;&amp; bash scripts/check-applescript-confirmation.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "devSnapshotEmitter\\.apply" App/AppDelegate.swift` returns at least 1 (replaces no-op drain)
    - `grep -c "var devSnapshotEmitter:" App/AppDelegate.swift` returns at least 1 (ivar declared)
    - `grep -c "var devOverlayBridge:" App/AppDelegate.swift` returns at least 1 (ivar declared)
    - `grep -c "bridge.attach(channel:" App/AppDelegate.swift` returns at least 1
    - `grep -c "for await _ in devSub.stream" App/AppDelegate.swift` returns 0 (the no-op drain pattern is GONE)
    - File `packages/DevOverlay/Tests/DevOverlayTests/DevOverlayBroadcasterIntegrationTests.swift` exists with at least 100 lines
    - `swift test --package-path packages/DevOverlay` exits 0
    - DevOverlayBridge.swift NOT MODIFIED: `git diff --name-only HEAD~1 -- packages/DevOverlay/Sources/DevOverlay/DevOverlayBridge.swift` returns empty (D-16 / AP-13)
    - DevOverlayViewModel.swift NOT MODIFIED: `git diff --name-only HEAD~1 -- packages/DevOverlay/Sources/DevOverlay/DevOverlayViewModel.swift` returns empty (D-16 / AP-13)
    - DevSnapshotEmitter.swift NOT MODIFIED: `git diff --name-only HEAD~1 -- packages/AgentCore/Sources/AgentOrchestrator/DevSnapshotEmitter.swift` returns empty (D-16)
    - `bash scripts/check-app-builds.sh` exits 0
    - All 18 boundary gates exit 0
  </acceptance_criteria>
  <done>Surgical fix landed in commit 2. DevOverlayBridge / DevOverlayViewModel / DevSnapshotEmitter unchanged (AP-13 honored). Integration test salvaged + GREEN. All gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verification — relaunch + send a tool-using turn + observe DevOverlay populates within 100ms</name>
  <read_first>
    - .planning/phases/10-self-awareness-diagnostics/10-RESEARCH.md (lines 968-998 — Live-Launch Verification Format D-26 template)
    - .planning/phases/10-self-awareness-diagnostics/10-VALIDATION.md (Manual-Only Verifications: "DevOverlay populates within 100ms of .turnEnded")
    - .planning/phases/10-self-awareness-diagnostics/10-CONTEXT.md (D-26, D-28 — tccutil reset is safety-only for plans that touch mic/camera; 10-03's verification depends on a live turn that triggers the mic chain — INCLUDE the reset for safety)
  </read_first>
  <what-built>
    - `.planning/phases/10-self-awareness-diagnostics/10-03-INVESTIGATION.md` (commit 1 — investigation-only)
    - AppDelegate surgical fix wiring DevSnapshotEmitter and bridge (commit 2)
    - Salvaged DevOverlayBroadcasterIntegrationTests from stash
    - All 18 boundary gates green
  </what-built>
  <how-to-verify>
    Per D-26 template — copy the section verbatim into `.planning/phases/10-self-awareness-diagnostics/10-03-SUMMARY.md`.

    Per D-28: Plan 10-03 doesn't strictly require tccutil reset (its verification doesn't depend on hardware capture surfaces being fresh), BUT the verification path involves sending a turn that may trigger the mic chain. INCLUDE the reset for safety:

    Reset:
    - `tccutil reset Microphone com.koftwentytwo.jarvis`
    - `tccutil reset Camera com.koftwentytwo.jarvis`

    Pre-checks:
    1. `bash scripts/check-app-builds.sh` PASS
    2. All 18 boundary gates green
    3. `swift test --package-path packages/DevOverlay` PASS

    Relaunch:
    - `bash scripts/check-app-builds.sh && open build/Build/Products/Debug/Jarvis.app`
    - Capture build artifact path + commit short-sha at `git rev-parse --short HEAD`
    - Click Allow on mic and camera prompts when they fire (verifies installVoice/installVision TCC paths)

    Live verification (AC-08):
    1. Open the DevOverlay window via the menu bar item.
    2. Confirm initial state: fields show empty/zero (consistent with no turns having completed yet).
    3. Send a TEXT turn via the HUD chat panel (text path is fine — voice not required for AC-08): "What time is it?"
    4. Wait for the model to invoke the `get_time` MCP tool (visible in agent log channel `subsystem == "com.koftwentytwo.jarvis"` with `category == "agent"`). Note the timestamp of the `.turnEnded` event.
    5. Within 100 ms of `.turnEnded`, observe the DevOverlay fields populate:
       - `Provider: anthropic`
       - `Model: claude-opus-4-7`
       - `Input: N tok` (non-zero)
       - `Output: N tok` (non-zero)
       - `Cache creation: N tok` and `Cache read: N tok (X% hit)` (zero-on-first-turn is expected; hit % populates after warmup)
       - `Latency: ttfb=Nms total=Nms` (both non-zero)
       - Last 5 tool calls list shows `get_time` with its actual duration
    6. Take a screenshot of the populated DevOverlay (or copy the field values verbatim into the SUMMARY).

    Bonus opportunistic verification: while DevOverlay is populated, ALSO retest Plan 10-02's AC-05 — ask "what mic are you using?" and observe `get_active_audio_route` appears in the last-5 tool calls list. This closes the cross-plan dependency Plan 10-02 carry-forward marker.

    Write the populated `## Live Verification (D-26 / SPEC AC-13)` section into 10-03-SUMMARY.md. The block MUST contain:
    - Pre-checks block
    - Reset block (with the tccutil reset commands run + observed prompts)
    - Relaunch block with build artifact + commit short-sha
    - Observed block: at least 4 timestamped log lines spanning the turn's lifecycle (turn start, tool_use for get_time, tool_result, turn_end)
    - Field-value capture: the six DevOverlay fields with their non-zero values
    - Acceptance Match block: `AC-08: PASS — <evidence>`. (And optionally `AC-05: PASS` if cross-verified.)
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/10-self-awareness-diagnostics/10-03-SUMMARY.md` contains heading `## Live Verification (D-26 / SPEC AC-13)`
    - SUMMARY contains `AC-08: PASS`
    - SUMMARY records all six DevOverlay field values (Provider, Model, Input, Output, Cache, Latency) populated with non-default values
    - SUMMARY records at least 4 timestamped log lines from `log stream --predicate 'subsystem == "com.koftwentytwo.jarvis"'`
    - SUMMARY records the tccutil reset commands and the observed mic/camera prompts (or notes they were already granted)
    - All 18 boundary gates exit 0 at commit-of-record
  </acceptance_criteria>
  <resume-signal>Type "approved" once AC-08 PASS in the Live Verification section, with all six DevOverlay fields populated within 100ms of .turnEnded. If a field stays zero, describe which one + the timestamps observed so the executor can iterate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broadcaster (untrusted-sized event stream) → DevSnapshotEmitter | Bounded by capacity:32 (AGENT-10 four-seam compliance); back-pressure via the bounded channel — events drop if the consumer can't keep up. |
| DevSnapshotEmitter.output → DevOverlayBridge | Bounded channel; viewModel held weakly by bridge, bridge held strongly by AppDelegate — subscriber-task lifetime bound to window lifetime via AppDelegate ivar (RESEARCH Example 6 lifecycle note). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-DEVOVERLAY-01 | Tampering / Quality | Wiring repair leaks scope into refactoring DevOverlayBridge or DevOverlayViewModel | mitigate | D-15/D-16 mandatory two-step pattern (investigation FIRST, surgical fix SECOND); AP-13 explicitly forbids refactor; acceptance criteria assert `DevOverlayBridge.swift` and `DevOverlayViewModel.swift` are unmodified at HEAD. RESEARCH Example 6 surgical diff is the only allowed change shape. |
| T-10-DEVOVERLAY-02 | Denial of Service | Subscriber Task deallocated mid-stream → DevOverlay silently goes dark | mitigate | RESEARCH Example 6 lifecycle note: bridge held strongly on `self.devOverlayBridge` ivar; emitter held strongly on `self.devSnapshotEmitter` ivar. View-model held weakly by bridge (correct direction). Acceptance asserts both ivars present in AppDelegate. |
| T-10-DEVOVERLAY-03 | Information Disclosure | DevOverlay snapshot leaks token counts / latency / model identity | accept | This is the explicit DIAG-02 goal — surface diagnostic info to the developer. No transcript content, no API keys, no PII. The window is dev-only and gated behind a menu item. Risk accepted. |
</threat_model>

<verification>
- Commit 1 (INVESTIGATION.md) lands alone with no source changes (D-15 / AP-16)
- Commit 2 (surgical fix + salvaged integration test) makes `swift test --package-path packages/DevOverlay` PASS
- DevOverlayBridge.swift / DevOverlayViewModel.swift / DevSnapshotEmitter.swift unmodified (D-16 / AP-13)
- `bash scripts/check-app-builds.sh` PASS; all 18 boundary gates green
- `.planning/phases/10-self-awareness-diagnostics/10-03-SUMMARY.md` contains `## Live Verification` with AC-08 PASS and all six DevOverlay fields captured non-zero within 100ms of .turnEnded
</verification>

<success_criteria>
- DevOverlay populates Provider/Model/Input/Output/Cache/Latency/last-5-tool-calls within 100ms of `.turnEnded` for any turn that completes
- The fix is surgical: 2 sites in AppDelegate.swift, 2 new ivars, 1 salvaged integration test. No refactor of bridge/viewModel/emitter
- Two-commit sequence respected (investigation → fix), traceable via git log
- Cross-plan: Plan 10-02's AC-05 (which depends on DevOverlay being populated) becomes verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/10-self-awareness-diagnostics/10-03-SUMMARY.md` with:
- Standard summary template (objective, work delivered, files touched, tests added/changed, gates run)
- A `## Live Verification (D-26 / SPEC AC-13)` section with AC-08 marked PASS, the six DevOverlay field values captured, and the timestamped log evidence inline
- A reference to the two-commit sequence (commit 1 = INVESTIGATION.md; commit 2 = surgical fix + salvaged test)
</output>
