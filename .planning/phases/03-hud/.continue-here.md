---
context: phase
phase: 03-hud
task: null
total_tasks: null
status: wave_3_merged_phase_verify_pending
last_updated: 2026-04-24T16:05:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Xcode 26 xctest harness Team-ID bug — `xcodebuild test -scheme Jarvis` fails with Runningboard error 5 / Launchd spawn failed on ad-hoc signed Debug bundles — DO NOT re-attempt fixing. Multiple workarounds tried (HR off, --options=runtime strip, debug.dylib delete, DEVELOPMENT_TEAM move, SWIFT_ENABLE_DEBUG_DYLIB=NO) — all dead ends. Root cause: `Jarvis.debug.dylib` gets a code signature whose Team-ID context diverges from the ad-hoc outer bundle. This is upstream Apple / Xcode 26.4. WAIT for Xcode 26.5.

**Do not proceed until the box above is checked (i.e., you've internalized the rule).**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-attempting Xcode 26 xctest fix | The pause/resume cycle tempts a fresh agent to "try one more thing" on the xctest launch failure. Four separate attempts burned ~3 hours. | blocking | `.planning/debug/xctest-launch-runningboard-error-5.md` documents the failure mode in full. If SPM tests pass and `xcodebuild build` succeeds, treat Phase 1 UAT item 4 (xctest launch) as "upstream-blocked, SPM coverage is authoritative" and move on. Phase 3 added `scripts/smoke-test-hud.sh` as the runtime-shape smoke substitute. |
| Skipping research fan-out | Tempting to execute Phase N immediately after planning Phase N. Research for downstream phases is banked — use it: plan Phase N+1 in parallel with executing Phase N. | advisory | The `.planning/phases/{04..08}/`-RESEARCH.md files are already committed. Re-spawn gsd-planner for any un-planned phase at any time. |
| Touching STATE.md / ROADMAP.md from executor worktree | Parallel executors writing STATE.md race. | blocking | Executors have FILE SCOPE instructions enforcing this. Orchestrator snapshots both files pre-merge and restores post-merge. See code-review-fix and execute-plan workflows. |
| Parallel-executing wave plans that share a modified file or import each other's outputs | Wave 2 originally planned 03-03 + 03-04 in parallel. But 03-04's App.tsx imports `LoadingFallbacks` from 03-03 and both modify App.tsx. `depends_on` frontmatter doesn't capture source-level cross-references. A parallel run would have either (a) required fabricating stubs on the 03-04 side or (b) merged with an App.tsx conflict. | advisory | Before spawning parallel wave executors, scan the target plans' `files_modified` lists for overlap and do a grep for each plan's NEW files being imported by the other. If overlap or cross-reference exists, sequentialize within the wave. Record the override in HANDOFF `wave_N_deviations`. |
| Assuming Agent-tool worktree isolation never auto-merges | Wave 3 (single plan 03-05) completed and the Agent-tool's worktree isolation auto-FF-merged the worktree branch into develop on completion. Waves 1 & 2 required manual `git merge --ff-only`. The post-Agent behavior is not deterministic — ALWAYS check `git log` on develop after the Agent returns before assuming merge-back is needed. | advisory | After each isolation="worktree" Agent completes, run `git log --oneline -N` on develop. If the agent's final SHA is already on develop, skip the manual merge; just clean up the stale worktree branch. If not, `git merge --ff-only worktree-agent-XXX`. The final SHA the agent reports is authoritative. |

<current_state>
**Phase 3 (HUD) — ALL 5 PLANS MERGED. Code-complete, phase-verify pending.** Tree clean on `develop` at `4643e14`. No worktrees active.

Phase 1 (Foundations) closed with 4 human-needed UAT items (all upstream). Phase 2 (Bus) closed clean. Phase 3 all 3 waves merged: 03-01 + 03-02 + 03-03 + 03-04 + 03-05. All 5 Phase 3 REQ-IDs (HUD-01, HUD-02, HUD-07, HUD-08, TEXT-02) have code + test + structural coverage. Phase 4 (Agent Core) has 5 plans written, ready to execute after Phase 3 closes via `/gsd-verify-phase 3`.

Tests passing / smoke:
- SPM: 103 (Keychain 6 + Config 16 + Logging 14 + Shell 20+1skipped + Bus 47) — unchanged
- App XCTest: compile-clean across all files including new WebviewBundleLoadTests + HudStateCoordinatorBusWiringTests; runtime still blocked by upstream Xcode 26 harness bug
- Webview vitest: 107 total (bus 28 + hud 79)
- Self-tests: 5/5
- Build smoke: `xcodebuild build -configuration Debug` → BUILD SUCCEEDED; `scripts/build-webview.sh` exit 0; `scripts/check-single-writer-hudstate.sh` exit 0; `scripts/smoke-test-hud.sh` PASS

Bundle: `webview/packages/hud` main chunk 1,084 kB (gzip 298 kB) — 8% over 1 MB soft ceiling, flagged for P8. Committed `App/Resources/webview/index.html` (649 bytes); `assets/` regenerated every build by `scripts/build-webview.sh`.

Research for Phases 5-8 banked (MCP 1069 lines, Voice 740, Memory+Vision 290, Hardening 827).
</current_state>

<completed_work>

### Phase 1 Foundations — CLOSED (code-complete, 4 HUMAN-UAT items)
- 5/5 plans done, 17/17 code-review findings resolved, bundle ID renamed to `com.koftwentytwo.jarvis`, Developer ID Application cert `MMLT Holdings LLC (7UC2HETAN9)` installed.

### Phase 2 Bus — CLOSED clean
- 4/4 plans done, verified `passed` (5/5 must-haves). Schema drift foreclosed by 5 independent defenses.

### Phase 3 HUD Wave 1 — MERGED
- 03-01: Swift HudStateCoordinator @MainActor single-writer, 7-state precedence, 25 XCTest methods. Commits: `ca17844`, `7909226`, `de763d5`.
- 03-02: webview/packages/hud React 19.2 + R3F 9.6.0 + drei 10.7.6 + three r184 + Zustand 5 + Vite 8. `useJarvisStore` + bus dispatcher + 12 vitest tests. Commits: `33e1afc`, `b9755b1`, `9a66569`, `b204307`.

### Phase 3 HUD Wave 2 — MERGED (sequential — see Anti-Patterns)
- 03-03: shader-driven animated particle ring — drei shaderMaterial with `?raw` GLSL imports, 7-state uniform map, 150ms crossfade, Reduce Motion fallbacks (shader `uReduceMotion` + CSS `@media` defense-in-depth), `useJarvisStore.getState()` in useFrame. 54 tests. Commits: `d094b3b`, `4951bfd`, `4138008`.
- 03-04: chat panel + streaming tokens — `ChatPanel` (role=log, aria-live=polite), exhaustive `ChatEventRouter`, 5-state `ToolCallCard` with `awaitingApproval` args-hidden defense-in-depth, bus dispatcher extended, client-side text-part id synthesis, 20-message replay-fixture chronology test. 79 tests. Commits: `e2dcaed`, `4e2e05e`, `424f698`.

### Phase 3 HUD Wave 3 — MERGED
- 03-05: bundle integration + end-to-end wiring. `scripts/build-webview.sh` (content-hash sentinel cache + rsync, preserves bus-harness.html); project.yml preBuildScripts (build-webview → single-writer lint → entitlement pre-verify → codesign → entitlement post-verify); `App/HUD/HudStateBridge.swift` (`busHudState(from:)` rawValue-bridge); `AppDelegate.installBus()` loads `index.html` instead of `bus-harness.html`, constructs + starts `HudStateCoordinator` with 3 dormant AsyncStream producers, wires emit → bridge.send, calls `markReady()` on handshake armed; `scripts/check-single-writer-hudstate.sh` activated + allowlist extended to 3 files; `WebviewBundleLoadTests` + `HudStateCoordinatorBusWiringTests` (structural, not xctest-launch); `scripts/smoke-test-hud.sh`. Auto-merged via Agent-tool worktree isolation. Commits: `a9a1b86`, `cabc272`, `b7255c5`, `4643e14`.

### Phase 4 Agent Core — PLANNED (not executed)
- 5 plans (04-01 LLMProvider+AnthropicProvider, 04-02 OllamaProvider, 04-03 Replay, 04-04 AgentOrchestrator, 04-05 DevOverlay+TEXT-01 E2E). 4 waves.
</completed_work>

<remaining_work>

### Phase 3 verify → close
- `/gsd-code-review 3` (code review over all 5 plans' diffs)
- `/gsd-code-review-fix 3` if findings
- `/gsd-verify-phase 3` → confirms all 5 REQ-IDs satisfied; refreshes STATE.md / ROADMAP.md (STATE.md is currently Phase-1-era; verify step will roll it forward to Phase-3-closed)
- Optional: manual HUD UAT per 03-05 SUMMARY §Manual UAT Checklist (cold-launch Jarvis.app from DerivedData, summon via menu-bar, observe ring .booting → .idle, check system.log for `bus handshake armed`)
- After verify passes: `/gsd-execute-phase 4` (Wave 1: 04-01 alone — AnthropicProvider sets the protocol shape used by all later providers)

### Phases 5-8
- `/gsd-plan-phase 5`, `/gsd-plan-phase 6`, `/gsd-plan-phase 7`, `/gsd-plan-phase 8`. Research is banked; can run in parallel with Phase 4 execution.
</remaining_work>

<decisions_made>

- **Bundle ID `com.koftwentytwo.jarvis`** (owner: KofTwentyTwo), commit `206b4aa` on 2026-04-22.
- **Developer ID Application cert installed:** MMLT Holdings LLC (7UC2HETAN9), fingerprint `4E9DC833E2B4C1C9ABDA96447177BF5EA8851FD6`.
- **Entitlements split** Debug vs Release. Debug drops Hardened Runtime, `--options=runtime`, and `speech-recognition-assets`. Commit `0517926`.
- **Hand-written Codable for discriminated unions** throughout Bus package — synthesized SE-0295 produces wrong wire shape.
- **MainActor.assumeIsolated workaround** for WKScriptMessageHandlerWithReply.
- **OutboundBatcher as actor + `Task.sleep(for: .milliseconds(33))`** — beats CADisplayLink / DispatchSourceTimer / AsyncThrottleSequence.
- **drei shaderMaterial + Vite `?raw` GLSL imports** — one shader, 7 states via uniforms. Commit `d094b3b`.
- **`useJarvisStore.getState()` inside useFrame**, never the hook — Pitfall 1 guard; lint enforced by `<verify>` grep. Commit `4951bfd`.
- **Two-path Reduce Motion defense:** shader `uReduceMotion` + CSS `@media` on DOM fallbacks.
- **Client-side text-part id synthesis `turn:{id}:assistant[:n]`** — Phase 4 orchestrator will emit explicit `chat/textStart`.
- **`awaitingApproval: true` sentinel hides args in `ToolCallCard`** — defense-in-depth for Pitfall 9. Commit `e2dcaed`.
- **Non-tokenDelta events close the active text-part** — ensures tool-call interleaves produce separate text-parts.
- **Commit `App/Resources/webview/index.html`, gitignore `App/Resources/webview/assets/`** — stable entry-point committed; hashed asset chunks regenerate every build. Commit `a9a1b86`.
- **Pre-build script chain:** build-webview.sh → check-single-writer-hudstate.sh → verify-entitlements --pre-codesign → codesign.sh → verify-entitlements --post-codesign. Commit `a9a1b86`.
- **Three dormant AsyncStream continuations retained by AppDelegate** — prevents Swift 6 continuation-drop from terminating the coordinator's for-await Tasks. Phase 4/5/6 replace with real producers. Commit `cabc272`.
- **3-file single-writer allowlist:** HudStateCoordinator.swift + HudStateBridge.swift + AppDelegate.swift. Commit `cabc272`.
- **`webviewEntryFilename` internal property as test seam** instead of subclassing JarvisHUDPanel. Commit `cabc272`.
- **Phase 4 will hand-roll URLSession+SSE+NDJSON** for both LLM providers — no Anthropic SDK dep.
- **Replay log hand-rolled SQLite WAL** — need `load_extension` for sqlite-vec in Phase 7.

Detailed commit history: git log --oneline on `develop` shows 70+ commits across Phases 1-3.
</decisions_made>

<blockers>
None active on Phase 3 verify path. All 4 HUMAN-UAT items from Phase 1 are upstream / external, don't block Phase 3 close or Phase 4 start.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — Phase-1-era; `/gsd-verify-phase 3` will roll forward
2. `.planning/ROADMAP.md` — 8-phase roadmap
3. `.planning/PROJECT.md` — core value, v1 definition
4. `.planning/phases/01-foundations/01-HUMAN-UAT.md` — 4 open human-verification items (all upstream)
5. `.planning/debug/xctest-launch-runningboard-error-5.md` — **required reading before any xctest-related decision**
6. `.planning/phases/03-hud/03-RESEARCH.md` — stack decisions
7. `.planning/phases/03-hud/03-0{1..5}-SUMMARY.md` — per-plan decisions/deviations/known stubs for Phase 3
8. `.planning/phases/04-agent-core/04-*-PLAN.md` — 5 plans queued for Phase 4 execution
9. `./CLAUDE.md` — architectural decisions (Swift 6 strict concurrency, Opus 4.7 footguns, Ollama transport, tool-choice discipline)

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN**: Attempting to fix the `xcodebuild test` Team-ID / dyld mismatch on Debug bundles → upstream-blocked; SPM + structural XCTests + `scripts/smoke-test-hud.sh` cover correctness.
- **ANTI-PATTERN**: Executing a plan without reading the plan's RESEARCH.md first.
- **ANTI-PATTERN**: Modifying STATE.md or ROADMAP.md from inside a worktree executor.
- **ANTI-PATTERN**: Using `--deep` in codesign flows.
- **ANTI-PATTERN** (wave 2 lesson): Parallel-executing wave plans that share a modified file or import each other's new files. Scan `files_modified` + grep cross-plan imports first.
- **ANTI-PATTERN** (wave 3 lesson): Assuming Agent-tool worktree isolation never auto-merges. Always `git log` develop after each Agent returns; merge manually only if the agent's final SHA isn't already on develop.

## Infrastructure State

- **Git:** tree clean on `develop` at `4643e14`. No worktrees active, no uncommitted changes.
- **Xcode:** 26.4.1 build 17E202. Swift 6.3.1. macOS SDK 26.4. Host macOS 26.3.1.
- **Toolchain:** xcodegen 2.45.4. SourceKit-LSP bundled.
- **Codesigning:** 1 valid identity in login Keychain (`Developer ID Application: MMLT Holdings LLC (7UC2HETAN9)`).
- **Dev tools:** pnpm + vitest + Vite 8 in `webview/`. swift-log, drei, R3F, Zustand all via SPM / npm.
- **GSD tools:** `~/.claude/get-shit-done/bin/gsd-tools.cjs` is the CLI.
- **Build pre-req:** `@jarvis/bus` TS package must be built before `@jarvis/hud` on a fresh `pnpm install`. `scripts/build-webview.sh` now handles this for every build.
- **Built app:** `~/Library/Developer/Xcode/DerivedData/Jarvis-*/Build/Products/Debug/Jarvis.app` has `Contents/Resources/webview/{index.html, assets/*.js, bus-harness.html}`; smoke script validates shape.
- **No background processes.** No worktrees. No in-flight agents.

## Pre-Execution Critique Required

_(not applicable — Phase 3 is code-complete, next step is code-review / verify)_

<context>
Mental state: Phase 3 fully executed (5/5 plans merged through 3 waves). Tree clean on develop at `4643e14`. `xcodebuild build -configuration Debug` succeeds, SPM Bus 47/47 green, hud vitest 79/79 green, smoke script passes. The single-writer lint is active as a pre-build script; can't land Phase 4 code that violates it without failing a build.

Approach on resume:
1. Read `.planning/phases/03-hud/03-0{1..5}-SUMMARY.md` for per-plan decisions.
2. `/gsd-code-review 3` — covers diffs across all 5 plans (Swift + TS + shell + YAML).
3. `/gsd-code-review-fix 3` if findings.
4. `/gsd-verify-phase 3` — validates all 5 REQ-IDs; refreshes STATE.md.
5. `/gsd-execute-phase 4` — Wave 1 is 04-01 alone (AnthropicProvider sets the protocol shape).
6. Plan-phases for 5-8 can run in parallel with Phase 4 execution.

Known good patterns from this session:
- **Fan out parallel planners aggressively** — planning for Phases N+1..8 doesn't touch source.
- **Be explicit about FILE SCOPE in executor prompts** — include files_modified verbatim.
- **Snapshot STATE.md + ROADMAP.md before merges** — restore after. Orchestrator did this for each wave; all snapshots matched post-merge byte-for-byte.
- **Use `--no-verify` for executor commits** — pre-commit hooks race in parallel worktrees.
- **Build `@jarvis/bus` before running `@jarvis/hud` test/typecheck/build after any fresh `pnpm install`** — scripts/build-webview.sh encodes this.

Known rough edges to expect:
- Research agents can stall at "now let me write the file" step for large outputs. Prompt with hard line limits.
- Planners sometimes notice the ROADMAP Plans: list isn't populated — orchestrator manually backfills after each plan-phase.
- Wave 2 lesson: static `depends_on` captures architectural deps but not source-level imports. Scan `files_modified` overlap + grep for cross-plan imports before spawning parallel executors.
- Wave 3 lesson: Agent-tool worktree isolation may auto-merge the branch into develop on clean exit. Check `git log` first before attempting a manual `git merge --ff-only`.
</context>

<next_action>
Start with:

```bash
/gsd-code-review 3
```

This runs code-review over all Phase 3 diffs (Swift + TS + shell + YAML). Expected depth `standard` per `.planning/config.json`. If findings land: `/gsd-code-review-fix 3`. Then:

```bash
/gsd-verify-phase 3
```

This closes Phase 3: validates all 5 REQ-IDs, refreshes STATE.md / ROADMAP.md, and emits the phase-closure artifacts. After verify passes, advance via `/gsd-execute-phase 4`.

If you want to fan out planners while code-review runs:
- `/gsd-plan-phase 5` in parallel (writes to `.planning/phases/05-mcp/`)
- `/gsd-plan-phase 6` in parallel (writes to `.planning/phases/06-voice/`)

Both planners run concurrently with the code-review agent.
</next_action>
