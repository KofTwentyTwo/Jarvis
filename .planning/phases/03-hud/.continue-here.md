---
context: phase
phase: 03-hud
task: null
total_tasks: null
status: wave_2_merged_wave_3_pending
last_updated: 2026-04-24T14:45:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Xcode 26 xctest harness Team-ID bug — `xcodebuild test -scheme Jarvis` fails with Runningboard error 5 / Launchd spawn failed on ad-hoc signed Debug bundles — DO NOT re-attempt fixing. Multiple workarounds tried (HR off, --options=runtime strip, debug.dylib delete, DEVELOPMENT_TEAM move, SWIFT_ENABLE_DEBUG_DYLIB=NO) — all dead ends. Root cause: `Jarvis.debug.dylib` gets a code signature whose Team-ID context diverges from the ad-hoc outer bundle. This is upstream Apple / Xcode 26.4. WAIT for Xcode 26.5.

**Do not proceed until the box above is checked (i.e., you've internalized the rule).**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-attempting Xcode 26 xctest fix | The pause/resume cycle tempts a fresh agent to "try one more thing" on the xctest launch failure. Four separate attempts burned ~3 hours. | blocking | `.planning/debug/xctest-launch-runningboard-error-5.md` documents the failure mode in full. If SPM tests pass and `xcodebuild build` succeeds, treat Phase 1 UAT item 4 (xctest launch) as "upstream-blocked, SPM coverage is authoritative" and move on. |
| Skipping research fan-out | Tempting to execute Phase N immediately after planning Phase N. Research for downstream phases is banked — use it: plan Phase N+1 in parallel with executing Phase N. | advisory | The `.planning/phases/{04..08}/`-RESEARCH.md files are already committed. Re-spawn gsd-planner for any un-planned phase at any time. |
| Touching STATE.md / ROADMAP.md from executor worktree | Parallel executors writing STATE.md race. | blocking | Executors have FILE SCOPE instructions enforcing this. Orchestrator snapshots both files pre-merge and restores post-merge. See code-review-fix and execute-plan workflows. |
| Parallel-executing wave plans that share a modified file or import each other's outputs | Wave 2 originally planned 03-03 + 03-04 in parallel. But 03-04's App.tsx imports `LoadingFallbacks` from 03-03 and both modify App.tsx. `depends_on` frontmatter doesn't capture source-level cross-references. A parallel run would have either (a) required fabricating stubs on the 03-04 side or (b) merged with an App.tsx conflict. | advisory | Before spawning parallel wave executors, scan the target plans' `files_modified` lists for overlap and do a grep for each plan's NEW files being imported by the other. If overlap or cross-reference exists, sequentialize within the wave. Record the override in HANDOFF `wave_N_deviations`. |

<current_state>
**Phase 3 (HUD) — Wave 2 merged, Wave 3 pending.** Tree clean on `develop` at `424f698`. No worktrees active.

Phase 1 (Foundations) closed with 4 human-needed UAT items (all upstream). Phase 2 (Bus) closed clean (status=passed). Phase 3 Waves 1+2 merged (plans 03-01 through 03-04). Only 03-05 (bundle integration + end-to-end smoke) remains in Phase 3. Phase 4 (Agent Core) has 5 plans written, ready to execute after Phase 3 closes.

Tests passing:
- SPM: 103 (Keychain 6 + Config 16 + Logging 14 + Shell 20+1skipped + Bus 47) — unchanged by wave 2
- App XCTest: 25 compile-clean (runtime blocked by upstream Xcode 26 bug — see constraint above)
- Webview vitest: **107 total** (bus 28 + hud 79) — hud up from 40 at wave 1 close
- Self-tests: 5/5 (entitlements fault-injection, bus protocol version, no-evaluateJavaScript, bus harness parity, hudstate single-writer)

Bundle size: `webview/packages/hud` main chunk `1,084 kB` (gzip `298 kB`) — 8% over 1 MB soft ceiling. R3F + three.js ≈60% of footprint. Dynamic-import code-split flagged for Phase 8 hardening.

Research for Phases 5-8 banked (MCP 1069 lines, Voice 740, Memory+Vision 290, Hardening 827).
</current_state>

<completed_work>

### Phase 1 Foundations — CLOSED (code-complete, 4 HUMAN-UAT items)
- 5/5 plans done, 17/17 code-review findings resolved, bundle ID renamed to `com.koftwentytwo.jarvis`, Developer ID Application cert `MMLT Holdings LLC (7UC2HETAN9)` installed.
- Pre-Phase-2 cleanup: entitlements split Debug/Release, codesign.sh config-aware, verify-entitlements.sh per-config MAIN_REQUIRED lists.

### Phase 2 Bus — CLOSED clean
- 4/4 plans done, verified `passed` (5/5 must-haves). Schema drift foreclosed by 5 independent defenses.
- Key artifacts: `packages/Bus/` Swift SPM, `webview/packages/bus/` TS, 3 preBuildScripts (check-bus-protocol-version, check-no-evaluate-javascript, check-bus-harness-parity).

### Phase 3 HUD Wave 1 — MERGED
- 03-01: Swift HudStateCoordinator @MainActor single-writer, 7-state precedence, lint-enforced (3-file allowlist), 25 XCTest methods. Commits: `ca17844`, `7909226`, `de763d5`.
- 03-02: webview/packages/hud React 19.2 + R3F 9.6.0 + drei 10.7.6 + three r184 + Zustand 5 + Vite 8. `useJarvisStore` + bus dispatcher + 12 vitest tests. Commits: `33e1afc`, `b9755b1`, `9a66569`, `b204307`.

### Phase 3 HUD Wave 2 — MERGED (sequential, not parallel — see Anti-Patterns)
- 03-03: shader-driven animated particle ring — drei shaderMaterial with `?raw` GLSL imports, 7-state uniform map, 150ms crossfade, Reduce Motion fallbacks (shader `uReduceMotion` + CSS `@media` defense-in-depth), `useJarvisStore.getState()` in useFrame (Pitfall 1 guard). 54 tests passing (27 stateUniforms + 15 RingMesh + 6 store + 6 bus-dispatch). Commits: `d094b3b`, `4951bfd`, `4138008`.
- 03-04: chat panel + streaming tokens — `ChatPanel` (role=log, aria-live=polite), `ChatEventRouter` exhaustive discriminator, `ToolCallCard` 5-state lifecycle with `awaitingApproval` sentinel hiding args, bus dispatcher extended for turnStarted/turnEnded/tokenDelta/toolCallStart/toolCallEnd, client-side text-part id synthesis `turn:{id}:assistant[:n]`, 20-message replay fixture with byte-normalized chronology snapshot. 79 tests passing (54 from 03-03 + 5 ChatPanel + 6 ToolCallCard + 12 streaming + 2 chronology). Commits: `e2dcaed`, `4e2e05e`, `424f698`.

### Phase 4 Agent Core — PLANNED (not executed)
- 5 plans (04-01 LLMProvider+AnthropicProvider, 04-02 OllamaProvider, 04-03 Replay, 04-04 AgentOrchestrator, 04-05 DevOverlay+TEXT-01 E2E). 4 waves.
</completed_work>

<remaining_work>

### Phase 3 HUD Wave 3
- 03-05 scripts/build-webview.sh + AppDelegate HudStateCoordinator wiring + index.html load + end-to-end smoke (HUD-01, HUD-08). Single plan — sequential.

### Phase 3 verify → close → advance to Phase 4
- `/gsd-code-review 3` → `/gsd-code-review-fix 3` if findings
- `/gsd-verify-phase 3` → if passed, close phase + update ROADMAP/STATE
- `/gsd-execute-phase 4` (Wave 1: 04-01 LLMProvider+AnthropicProvider alone)

### Phases 5-8
- Need `/gsd-plan-phase 5`, `/gsd-plan-phase 6`, etc. Research is banked, can run in parallel with phase 3/4 execution.
</remaining_work>

<decisions_made>

- **Bundle ID `com.koftwentytwo.jarvis`** (owner: KofTwentyTwo), commit `206b4aa` on 2026-04-22.
- **Developer ID Application cert installed:** MMLT Holdings LLC (7UC2HETAN9), fingerprint `4E9DC833E2B4C1C9ABDA96447177BF5EA8851FD6`.
- **Entitlements split** Debug (`Jarvis.Debug.entitlements` — no `speech-recognition-assets`, adds `get-task-allow`) vs Release (`Jarvis.Release.entitlements` — full managed set). Debug config drops Hardened Runtime and `--options=runtime` flag. DEVELOPMENT_TEAM only on Release. Commit `0517926` (2026-04-23).
- **Hand-written Codable for discriminated unions** throughout Bus package — synthesized SE-0295 produces wrong wire shape vs TS discriminator.
- **MainActor.assumeIsolated workaround** for WKScriptMessageHandlerWithReply (Apple Forum 751086 / FB13774556).
- **OutboundBatcher as actor + `Task.sleep(for: .milliseconds(33))`** — beats CADisplayLink / DispatchSourceTimer / AsyncThrottleSequence.
- **drei shaderMaterial + Vite `?raw` GLSL imports** — one shader, 7 states via uniforms. `grep uColorGlow` on built JS confirms `?raw` survives into bundle. Commit `d094b3b`.
- **`useJarvisStore.getState()` inside useFrame**, never the hook — Pitfall 1 guard; lint enforced by `<verify>` grep. Commit `4951bfd`.
- **Two-path Reduce Motion defense:** shader `uReduceMotion` uniform driven from Swift-emitted a11y bus message + CSS `@media (prefers-reduced-motion)` on the DOM fallbacks. If bus is delayed, CSS still catches.
- **Client-side text-part id synthesis `turn:{id}:assistant[:n]`** — Phase 2 `tokenDelta` doesn't carry an id; dispatcher synthesizes. Forward-compatible with Phase 4 when orchestrator emits explicit `chat/textStart`.
- **`awaitingApproval: true` sentinel in `argsPreview`** hides args in `ToolCallCard` — defense-in-depth for Pitfall 9 even though Phase 5 MCP-04 is the authoritative enforcer. Commit `e2dcaed`.
- **Non-tokenDelta events close the active text-part** — ensures tool-call interleaves between tokens produce separate text-parts (chronology preserved).
- **Phase 4 will hand-roll URLSession+SSE+NDJSON** for both LLM providers — no Anthropic SDK dep (D1 enum lag structural).
- **Replay log hand-rolled SQLite WAL** — need `load_extension` for sqlite-vec in Phase 7.

Detailed commit history: git log --oneline on `develop` shows 66+ commits across Phases 1-3.
</decisions_made>

<blockers>
None active on Phase 3 path. All 4 HUMAN-UAT items from Phase 1 are upstream / external, don't block Phase 3+ progress.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current position, locked key decisions (12 entries). **Note:** STATE.md header is still Phase-1-focused; `/gsd-verify-phase 3` will refresh it when Phase 3 closes.
2. `.planning/ROADMAP.md` — 8-phase roadmap with Plans: lists for Phases 1-3
3. `.planning/PROJECT.md` — core value, v1 definition
4. `.planning/phases/01-foundations/01-HUMAN-UAT.md` — 4 open human-verification items (all upstream)
5. `.planning/debug/xctest-launch-runningboard-error-5.md` — **required reading before any xctest-related decision**
6. `.planning/phases/03-hud/03-RESEARCH.md` — stack decisions (React 19.2 + R3F 9.6.0 + drei 10.7.6 + three r184 + Zustand 5 + Vite 8)
7. `.planning/phases/03-hud/03-05-bundle-integration-PLAN.md` — next up to execute
8. `.planning/phases/03-hud/03-03-SUMMARY.md` + `03-04-SUMMARY.md` — decisions/deviations/known stubs from wave 2
9. `.planning/phases/04-agent-core/04-*-PLAN.md` — 5 plans queued for Phase 4 execution
10. `./CLAUDE.md` — architectural decisions (Swift 6 strict concurrency, Opus 4.7 footguns, Ollama transport, tool-choice discipline)

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN**: Attempting to fix the `xcodebuild test` Team-ID / dyld mismatch on Debug bundles → accept upstream; SPM coverage is authoritative. Full diagnosis at `.planning/debug/xctest-launch-runningboard-error-5.md`.
- **ANTI-PATTERN**: Executing a plan without reading the plan's RESEARCH.md first → executor will make subtly wrong decisions (wrong wire format, wrong framework pattern). Every Phase 3-8 PLAN has a RESEARCH.md that pins decisions.
- **ANTI-PATTERN**: Modifying STATE.md or ROADMAP.md from inside a worktree executor → orchestrator handles these writes centrally; parallel writes race.
- **ANTI-PATTERN**: Using `--deep` in codesign flows → strips per-helper entitlements. `scripts/codesign.sh` does deepest-first manual walk; `scripts/verify-codesign-settings.sh` lints pbxproj to prevent regression.
- **ANTI-PATTERN** (wave 2 lesson): Parallel-executing wave plans that share a modified file or import each other's new files. Even when `depends_on` frontmatter says they're independent, scan `files_modified` lists for overlap and cross-imports first.

## Infrastructure State

- **Git:** tree clean on `develop` at `424f698`. No worktrees active, no uncommitted changes.
- **Xcode:** 26.4.1 build 17E202. Swift 6.3.1. macOS SDK 26.4. Host macOS 26.3.1.
- **Toolchain:** xcodegen 2.45.4. SourceKit-LSP bundled.
- **Codesigning:** 1 valid identity in login Keychain (`Developer ID Application: MMLT Holdings LLC (7UC2HETAN9)`).
- **Dev tools:** pnpm + vitest + Vite 8 in `webview/`. swift-log, drei, R3F, Zustand all via SPM / npm.
- **GSD tools:** `~/.claude/get-shit-done/bin/gsd-tools.cjs` is the CLI (not `gsd-sdk` — workflows reference `gsd-sdk query ...` but it resolves to `gsd-tools.cjs` here).
- **Build pre-req:** `@jarvis/bus` TS package must be `pnpm --filter @jarvis/bus build` before `@jarvis/hud` tests/typecheck/build run on a fresh `pnpm install` (workspace package publishes its `dist/` via `exports`). The wave-2 post-merge verification caught this.
- **No background processes.** No worktrees. No in-flight agents.

## Pre-Execution Critique Required

_(not applicable — we're between waves of an already-planned phase, not between design and execution)_

<context>
Mental state: user is driving, told me to push hard with background agents. Phase 1+2 are closed, Phase 3 Waves 1+2 done. Tree clean on develop at `424f698`. Next up is 03-05 (wave 3, single plan) which wires the R3F bundle into the Swift app and adds the end-to-end smoke test.

Approach on resume:
1. Read `.planning/STATE.md` + `.planning/ROADMAP.md` + `.planning/phases/03-hud/03-05-*-PLAN.md`.
2. `git rev-parse HEAD` to confirm base = `424f698`, then `/gsd-execute-phase 3 --wave 3` (or spawn the executor directly for 03-05 in worktree isolation).
3. After 03-05 merges: `/gsd-code-review 3` → `/gsd-code-review-fix 3` if findings → `/gsd-verify-phase 3`.
4. Close phase 3, advance to `/gsd-execute-phase 4` (5 plans, 4 waves — Phase 4 is the heaviest; Wave 1 is 04-01 alone because AnthropicProvider sets the protocol shape used by all later providers).
5. Plan-phases for 5-8 can run in parallel with Phase 4 execution (they write to separate dirs).

Known good patterns from this session:
- **Fan out parallel planners aggressively** — planning for Phases N+1..8 doesn't touch source, only `.planning/phases/NN-*/`
- **Be explicit about FILE SCOPE in executor prompts** — each plan lists files_modified, include them verbatim in the spawn prompt
- **Snapshot STATE.md + ROADMAP.md before merges** — restore after
- **Use `--no-verify` for executor commits** — pre-commit hooks race in parallel worktrees
- **Build `@jarvis/bus` before running `@jarvis/hud` test/typecheck/build after any fresh `pnpm install`** — workspace package's `exports` point at `dist/`

Known rough edges to expect:
- Research agents can stall at "now let me write the file" step for large outputs (Phase 4/6/7 stalled the first time). Prompt with "HARD LIMIT: N lines max. Write ONCE. Be terse." → shorter, more reliable.
- Planners sometimes notice the ROADMAP Plans: list isn't populated — orchestrator manually backfills after each plan-phase.
- Wave 2 lesson: static `depends_on` captures architectural deps but not source-level imports. Scan `files_modified` overlap + grep for cross-plan imports before spawning parallel executors; if overlap, sequentialize within the wave and record the deviation in HANDOFF.
</context>

<next_action>
Start with:

```bash
/gsd-execute-phase 3 --wave 3
```

This executes 03-05 (bundle integration — `scripts/build-webview.sh`, AppDelegate HudStateCoordinator wiring, `index.html` load, end-to-end smoke). Single plan, sequential. Expected ~30-45 min wall-clock.

If you want to fan out further while Wave 3 runs:
- `/gsd-plan-phase 5` in parallel (writes to `.planning/phases/05-mcp/` — no conflict)
- `/gsd-plan-phase 6` in parallel (writes to `.planning/phases/06-voice/`)

Both planners can run concurrently with the Phase 3 Wave 3 executor.
</next_action>
