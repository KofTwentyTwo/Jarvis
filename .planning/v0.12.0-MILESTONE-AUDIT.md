---
milestone: v0.12.0
audited: 2026-05-01
status: tech_debt
scores:
  requirements: 75/75 mapped (every REQ-ID in REQUIREMENTS.md traceability table is assigned to a phase whose programmatic must-haves pass; see Requirements Coverage section)
  phases: 8/8 implemented (all programmatic must-haves green; 4 phases carry operator-action items by design, 1 phase carries cross-phase wiring deferrals)
  integration: 4 cross-phase wiring deferrals (Phase 7 → AppDelegate dispatch points)
  flows: programmatic flows pass; 6 voice UAT flows blocked on Release-signed archive (Xcode 26 fragility)
nyquist:
  compliant_phases: [01-foundations, 08-hardening]
  partial_phases: []
  missing_phases: [02-bus, 03-hud, 04-agent-core, 05-mcp, 06-voice, 07-memory-vision]
  overall: partial
gaps:  # Critical blockers — none for ship; this milestone has zero programmatic regressions
  requirements: []
  integration:
    - id: INT-07-01
      from: AgentOrchestrator.events
      to: MemoryExtractionCoordinator.start
      site: App/AppDelegate.swift:641
      evidence: "agentOrchestratorEvents() returns nil (placeholder); coord.start() skipped. MemoryExtractionCoordinator type + tests exist; consumer is unwired."
      reason: "Documented in 07-06-SUMMARY 'Deferred Wiring' #1 as cross-phase blocker — the orchestrator events producer side lives in Phase 4/5 and was not threaded into Phase 7's consumer. Memory extraction therefore never runs in the live app even though the type is fully implemented."
      affected_requirements: [ME-01, ME-02, ME-03, ME-04, ME-05]
    - id: INT-07-02
      from: AgentOrchestrator.runTurn
      to: VisionRouter.route / evaluatePostResponse
      site: App/AppDelegate.swift:723-747
      evidence: "VisionRouter constructed and held strong on `visionRouter`; no AgentOrchestrator branch dispatches images through it. Tier ladder + escalation tests pass against the router type, but the dispatch path is absent."
      reason: "Documented in 07-VERIFICATION.md as 'Phase 4/5 orchestrator wiring + a follow-on dispatch plan'."
      affected_requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05]
    - id: INT-07-03
      from: ContextBuilder.installPresence
      to: per-turn system-prompt enrichment (D-10)
      site: packages/Vision/Sources/Vision/ContextBuilder.swift:47-56
      evidence: "installPresence is intentionally a no-op drain; bus and call site exist (AppDelegate.swift:708, AppDelegateContextBuilderAdapter.swift) but the consumer doesn't actually mutate the system prompt."
      reason: "Deferred to follow-on plan per 07-VERIFICATION.md."
      affected_requirements: [VIS-06, AGENT-09]
    - id: INT-07-04
      from: FrameAttachController
      to: AppDelegate
      site: App/AppDelegate.swift (controller never instantiated)
      evidence: "Type fully implemented in packages/Vision/Sources/Vision/FrameAttachController.swift; 5 unit tests + 3 grep-gate tests + 2 placeholder tests all green; never constructed in production app."
      reason: "07-06-SUMMARY 'Surprises Encountered' notes the constructor signature draft did not match 07-05; deferred to a future plan that finishes the runTurn dispatch + HUD camera-icon button."
      affected_requirements: [VIS-07]
  flows: []  # programmatic flows all green
tech_debt:
  - phase: 01-foundations
    items:
      - "SC-2 runtime probe still pending (7/8 success criteria verified — infrastructure 100% complete; final SC awaits a runtime sanity-check)"
  - phase: 03-hud
    items:
      - "Three dormant AsyncStream producers (agent / voice / confirmation) in AppDelegate — wired in subsequent phases per design, but the dormant-state pattern is worth documenting"
  - phase: 05-mcp
    items:
      - "VERIFICATION.md frontmatter still says `status: gaps_found` referencing the AppDelegate.swift:294 await-in-autoclosure bug — STALE. Fix landed in commit 5f9a767 (`fix(05-verify): G-01 extract await out of string interpolation autoclosure`). Current AppDelegate.swift:375 places the await in a proper async closure context. Plus all subsequent phases (06, 07, 08) executed end-to-end on top of Phase 5's wiring, which couldn't have happened without a compiling App target. Recommend re-running /gsd-verify-phase 5 (or close-with-evidence in Phase 5's VERIFICATION.md re_verification block)."
  - phase: 06-voice
    items:
      - "Six P6 HUMAN-UAT gates (VOICE-07 happy path, VOICE-09 AEC banner, VOICE-12 mute-wake-word + PTT-still-armed, VOICE-13 PTT, VOICE-14 barge-in, VOICE-10 mic re-grant) — code-side covered by deterministic XCTest cases; live UAT blocked by Xcode 26 ad-hoc Debug bundle launch fragility"
      - "Xcode 26 / Swift 6 / Info.plist / preview-dylib codesign fragility — `SWIFT_ENABLE_DEBUG_DYLIB=NO` set but ignored; codesign reports 'invalid Info.plist' after a clean build's BUILD SUCCEEDED. Workaround documented; UAT route is Release-signed Developer ID archive."
  - phase: 07-memory-vision
    items:
      - "Four cross-phase wiring deferrals captured in <gaps.integration> above (INT-07-01..04). All four block live behaviour even though the implementations are present — they're the highest-impact items in this audit."
  - phase: 08-hardening
    items:
      - "Operator-recorded wake-hysteresis corpus (per-host, real voice + ambient profiles) — synthetic seed proves pipeline wiring only; real FAR/FRR numbers require operator action per D-18 design. Now reported correctly thanks to the new PIPELINE/PERFORMANCE distinction (commit 5120a27)"
      - "Orpheus empirical TTFA measurement on Apple Silicon (target 150–250 ms, fall back to TTSKit if missed) — D-13 manual gate"
      - "AVAudioEngine synthetic device-change injection availability — D-12 unconfirmed; operator unplug/plug audio interface mid-utterance is the documented fallback"
      - "Live Anthropic SSE shape regression (jarvis-eval corpus-sse --live) — release-time only per cost concerns"
      - "Helper-side side-effect-marker emission (the follow-up to commit 5120a27 fix #7 — substrate is in ReplayMCPAdapter; helpers themselves don't emit yet)"
nyquist_audit:
  validation_md_present:
    - 01-foundations
    - 08-hardening
  validation_md_missing:
    - 02-bus
    - 03-hud
    - 04-agent-core
    - 05-mcp
    - 06-voice
    - 07-memory-vision
  recommendation: "Six phases lack VALIDATION.md. Run /gsd-validate-phase N for each missing phase if Nyquist sampling discipline matters at this milestone boundary; otherwise accept partial Nyquist coverage as tech debt and proceed."
state_md_drift:
  - "STATE.md frontmatter says `status: executing`, `completed_phases: 7`, `completed_plans: 35/39`, `percent: 90` — STALE. Phase 8 has all 4 SUMMARY.md files, VERIFICATION.md (status human_needed = phase complete with documented manual items), and VALIDATION.md (audited 2026-05-01). Recommend running /gsd-progress or `gsd-sdk query state.complete-phase 8` to flip the counters before milestone completion."
---

# v0.12.0 Milestone Audit

> Audited: 2026-05-01
> Status: **tech_debt** (no programmatic blockers; meaningful deferred items)
> Reviewer: Claude Opus 4.7 (gsd-audit-milestone)

This audit aggregates phase-level VERIFICATION.md and VALIDATION.md
artifacts for v0.12.0's eight phases. The integration checker step
(`gsd-integration-checker`) was synthesized inline rather than spawned —
all cross-phase signal is already captured in Phase 7's
VERIFICATION.md `deferred` block, which is exhaustive about which
AppDelegate dispatch points are stubbed.

## Executive Summary

**Where we are:** All eight phases land their programmatic must-haves
green. Cross-AI plan review (commit `4f321a7`) rated risk LOW with no
HIGH-severity concerns. Three lingering categories of work:

1. **Cross-phase wiring (real)** — Phase 7's four deferred wiring
   points actually block live behaviour: memory extraction never runs
   because `agentOrchestratorEvents()` returns nil; vision dispatch is
   missing from `runTurn`; context-prompt enrichment is a no-op;
   `FrameAttachController` is never instantiated. The types and tests
   are all present and green; the dispatch glue is missing.
2. **Operator-action items (by design)** — Six VOICE UAT gates,
   real-voice wake-corpus measurement, Orpheus TTFA timing, and
   AVAudioEngine device-change injection are all D-locked manual gates
   per Phase 6 and Phase 8. Not blockers; need execution against a
   Release-signed archive.
3. **Stale signals (cleanup)** — Phase 5's VERIFICATION.md still
   reports the AppDelegate await-in-autoclosure gap that was fixed in
   commit `5f9a767`. STATE.md still says `status: executing` /
   `completed_phases: 7`. Both should be flipped before
   `/gsd-complete-milestone v0.12.0`.

**Verdict:** ship-ready *if* the four Phase 7 dispatch deferrals are
either (a) accepted as tech debt for v0.13.x or (b) closed by a small
plan that wires them. The audit reports `tech_debt` rather than
`passed` because INT-07-01..04 represent **live functionality that the
code claims to implement but doesn't actually run** — that's a
qualitatively different category from the operator-action items.

## Per-Phase Verification Roll-Up

| Phase | Status | Score | Programmatic | Operator-action | Real Gaps |
|-------|--------|-------|--------------|-----------------|-----------|
| 01-foundations | human_needed | 7/8 SC | ✅ | SC-2 runtime probe | 0 |
| 02-bus | passed | full | ✅ | — | 0 |
| 03-hud | human_needed | 55/55 must-haves | ✅ | — | 0 |
| 04-agent-core | passed | full | ✅ | — | 0 |
| 05-mcp | gaps_found (STALE) | 31/32 | ✅ now | — | 0 (gap closed in `5f9a767`) |
| 06-voice | human_needed | 7/7 | ✅ | 6 UAT gates | 0 |
| 07-memory-vision | passed_with_deferrals | 13/13 | ✅ | — | 4 cross-phase wiring |
| 08-hardening | human_needed | 13/13 | ✅ | wake corpus / TTFA / etc. | 0 |

**Programmatic check totals across the milestone:** every phase's
"must-haves" verification block is fully green. The `human_needed` and
`passed_with_deferrals` labels reflect either operator-action items
locked by D-decisions or cross-phase wiring deferred to later plans —
neither category is a programmatic regression.

## Requirements Coverage

REQUIREMENTS.md traceability table maps **75 REQ-IDs** to phases. Every
REQ-ID assigned to a phase that has reached `passed`,
`passed_with_deferrals`, `human_needed`, or `gaps_found` (after the
stale-signal close-out) has at least one Plan SUMMARY referencing it.
The four Phase 7 cross-phase wiring deferrals (INT-07-01..04) affect
**six requirements** (ME-01..05, VIS-01..07, AGENT-09) at the
**dispatch-glue** layer — the *type-level* implementations are
verified, but the live app doesn't reach them.

No orphaned requirements detected.

## Cross-Phase Integration

Synthesized from Phase 7's VERIFICATION.md `deferred` block plus a
direct read of `App/AppDelegate.swift`:

| ID | From → To | Site | Live? |
|----|-----------|------|-------|
| INT-07-01 | AgentOrchestrator.events → MemoryExtractionCoordinator | AppDelegate.swift:641 | ❌ stub returns nil |
| INT-07-02 | AgentOrchestrator.runTurn → VisionRouter | AppDelegate.swift:723-747 | ❌ no dispatch branch |
| INT-07-03 | ContextBuilder.installPresence → system prompt | ContextBuilder.swift:47-56 | ❌ no-op drain |
| INT-07-04 | FrameAttachController → AppDelegate | (never constructed) | ❌ not instantiated |

These are the real cross-phase gaps. Closing them is a focused plan —
likely 2-4 tasks — that lives in a v0.13.x phase or a numbered insert
between Phase 8 and milestone close.

## Nyquist (Validation) Coverage

Validation discipline is partial across the milestone:

| Phase | VALIDATION.md | nyquist_compliant |
|-------|---------------|-------------------|
| 01-foundations | ✅ | true |
| 02-bus | ❌ missing | — |
| 03-hud | ❌ missing | — |
| 04-agent-core | ❌ missing | — |
| 05-mcp | ❌ missing | — |
| 06-voice | ❌ missing | — |
| 07-memory-vision | ❌ missing | — |
| 08-hardening | ✅ | true |

Phase 1 and Phase 8 are the bookends — they have explicit
VALIDATION.md files. The middle six phases never had Nyquist tracking
files emitted (likely a workflow-toggle issue at the time those phases
were planned). Sampling discipline isn't a ship blocker for a
single-machine personal-use project, but it's worth surfacing for
v0.13.x.

## Recommended Next Actions

The audit suggests three concrete clean-up commits before
`/gsd-complete-milestone v0.12.0`, ordered by ROI:

1. **Close Phase 5's stale gap** — re-run `/gsd-verify-phase 5` or
   manually append `re_verification` to Phase 5's VERIFICATION.md
   citing commit `5f9a767`. (5 min)
2. **Refresh STATE.md counters** — flip `status: executing → audited`,
   `completed_phases: 7 → 8`, `completed_plans: 35/39 → 39/39`. (2 min;
   ideally driven by `/gsd-progress` or a state-cli call.)
3. **Decide on INT-07-01..04** — either insert a phase to wire them up
   or accept as v0.13.x tech debt. **This is the only real decision.**

After those, the milestone is genuinely ready for archive.

---

*Generated by gsd-audit-milestone, 2026-05-01.*
